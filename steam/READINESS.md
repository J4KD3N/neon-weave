# Steam readiness

Generated by `tools/steam_check.gd` (S58). What the integration proves without a client, and what the live verification in `steam/CHECKLIST.md` still needs. `--strict` exits 1 while a blocker stands.

## Client and config

| Check | State |
|---|---|
| GodotSteam extension (`Steam` singleton) | absent: `addons/godotsteam/` is not in this checkout |
| steam/steam.json app_id | 0: unset |
| steam/steam.json demo_app_id | 0: unset |
| demo flag | true: this build is the demo |
| backend picked here | null |

## Achievements (define these names in Steamworks)

| Steam API name | Content id | Name |
|---|---|---|
| `ACH_ENDING_FIRE` | ending_ashfound_fire | The Fire |
| `ACH_ENDING_DRIFT` | ending_drift | The Drift |
| `ACH_ENDING_ORDER` | ending_lattice_order | Order |
| `ACH_ENDING_BLOOM` | ending_rootched_bloom | The Bloom |
| `ACH_ENDING_MEND` | ending_weavers_mend | The Weaver's Mend |
| `ACH_FIRST_CONTACT` | first_contact | It Knew Your Name |
| `ACH_FIRST_EXTRACTION` | first_extraction | First Extraction |
| `ACH_FULL_PARTY` | full_party | Four Weavers |
| `ACH_IRON_WEAVE` | iron_weave | Iron Weave |
| `ACH_MORTAL` | mortal | No Second Chances |
| `ACH_ROAD_OPEN` | road_open | The Road East |
| `ACH_SECOND_DEPTH` | second_depth | Where the Singing Starts |
| `ACH_WARLORD` | warlord | Throat Cleared |

13 achievements. Unlocking live is the checklist's step 5; here they unlock in the null backend only.

## Cloud

Files the game writes through the Remote Storage API: slot_1.json, slot_1.png, slot_2.json, slot_2.png, slot_3.json, slot_3.png, autosave.json, autosave.png, autosave_1.json, autosave_1.png, autosave_2.json, autosave_2.png (12). Set the quota to at least 16 MB for 12 files; thumbnails are 320×180 PNGs, saves a few hundred KB.

## Steam Input

Template binds: A, B, X, Y, dpad_down, dpad_left, dpad_right, dpad_up, select, shoulder_left, shoulder_right, start, trigger_left, trigger_right.
Every pad binding in `InputActions.BINDINGS` has a binding in `steam/steam_input_template.vdf`.

## Depots and launch

app_build.vdf: AppID unset, depots [0, 0, 0].
The demo app's launch option must pass `--demo`; the full game's passes nothing. Both take `--deck` and `--perf` for testing.

## Workshop

Installed items on this backend: 0. `tools/workshop_upload.gd -- --mod=<folder>` validates and publishes; the null backend publishes to `user://workshop_null/`.

## Verdict

Not ready. Blockers:
- GodotSteam is not installed here (addons/godotsteam/)
- steam/steam.json app_id is 0
- steam/app_build.vdf still has 4 unfilled ids

Notes:
- demo_app_id is 0: the demo depot needs its own app
