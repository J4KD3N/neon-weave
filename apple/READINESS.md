# Apple readiness

Generated by `tools/apple_check.gd` (S59). What the checkout proves about the macOS build, and what the live verification in `apple/CHECKLIST.md` still needs. `--strict` exits 1 while a blocker stands.

## The preset

| Check | State |
|---|---|
| bundle identifier | dev.neonweave.game |
| architecture | universal |
| versions | preset 1.0.0 / 1.0.0, project 1.0.0 |
| minimum macOS | x86_64 10.13, arm64 11.00 |
| signing in the committed preset | disabled (ad-hoc by Godot; a fork exports as before) |
| notarization in the committed preset | off (the release workflow turns it on when the secrets exist) |
| entitlements file | res://apple/entitlements.plist |

## Entitlements

| Entitlement | Granted |
|---|---|
| com.apple.security.cs.allow-dyld-environment-variables | no |
| com.apple.security.cs.allow-jit | no |
| com.apple.security.cs.allow-unsigned-executable-memory | yes |
| com.apple.security.cs.disable-library-validation | no |
| com.apple.security.device.audio-input | no |
| com.apple.security.device.camera | no |

## The release workflow

| Check | State |
|---|---|
| sign-and-notarize step | present |
| secrets the step reads | APPLE_CERTIFICATE_P12, APPLE_CERTIFICATE_PASSWORD, APPLE_API_KEY_P8, APPLE_API_KEY_ID, APPLE_API_ISSUER |
| rcodesign | installed by the step |
| Godot credential overrides | GODOT_MACOS_* exported |

Whether the five secrets exist in the repository cannot be read from a checkout: the step prints which it found at run time, and `apple/CHECKLIST.md` step 4 is where they are added.

## Verdict

The checkout is ready: a tag with the five secrets set produces a signed, notarized, stapled universal build. Steps 6 to 9 of the checklist need a Mac.

Notes:
- notarization is off in the preset by design; the release workflow enables it for a run with the APPLE_* secrets
