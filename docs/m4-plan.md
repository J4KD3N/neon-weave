# M4 plan — Polish and 1.0

Written at the close of M3 (S46, 2026-09-21). M3 made the game content complete: three acts wired end to end on every path, five endings, six companions, four romances, three factions, ten races, four biomes, seven buildings, modding docs and a validator, `v0.3.0` from one tag. M4 makes it good, and ships it. GDD §14: balance, Steam Deck performance, localization scaffolding, launch.

## What M4 is for

1. **Balance** over the whole game, with people. The harness (`test_balance.gd`) stands in for playtesters today; M4 starts with the first external playtest and reorders the rest by what it finds.
2. **The tracks that need people** land: art, audio, Steam, Apple. Each is a hand-off doc already (`docs/art-pipeline.md`, `docs/store-page.md`, `steam/`); M4 sessions integrate what comes back.
3. **Polish the surfaces**: the creator as a screen, dialogue with portraits, the pad as a first-class input, saves with slots and confirmations, accessibility.
4. **Ship**: notarized macOS, Steam depots, Workshop, `v1.0.0`.

## Sessions

| Session | Work | Done when |
|---|---|---|
| S47 | **First external playtest** and its triage: two people finish Act 1; findings ranked; `test_balance.gd` bands moved to what they found | A triage doc with owners; the harness bands match the playtest |
| S48 | **Balance pass 1, fights**: depth-3 extraction off the floor, the Key keepers and the Loom's Voice rowed and in band, the Cathedral survivable with retreat and heals, boss telegraphs | Every harness row in band; depth 3 extracts 40%+ naive |
| S49 | **Balance pass 2, economy and approval**: building costs, loot ranges, XP, approval per act so loyalty (4) and romance (5) are reachable where the story expects them | A walkthrough reaches approval 5 by the Keys without setting it |
| S50 | **Difficulty settings and Iron Weave**: three difficulties as rules overrides; Iron Weave (one save, Mortal, no reloads) as an account flag | Each difficulty is a rules entry; Iron Weave completes the campaign test |
| S51 | **Creator as a screen**: appearance overlays, portraits (placeholders), origins on New game, attribute preview of what a point buys | The creator is the first screen of a new game |
| S52 | **Dialogue and journal surfaces**: portraits in the panel, a log, quest tracking on the HUD, the Roster and Quarters as one screen | Every text panel has a face and a history |
| S53 | **Pad and Deck**: analog glide, rumble, the Deck's resolution and font sizes, a performance budget per map and Shard size | 60 fps on a Deck-class target in every biome at the largest Shard |
| S54 | **Saves and settings**: slots with names and thumbnails, overwrite confirmation, autosave rotation, music and SFX buses, accessibility (glyphs, text size, colour-blind palettes) | A settings screen a stranger can use |
| S55 | **Localization scaffolding**: every string through a table, a pseudo-locale test, the content kinds' text fields enumerated for translators | The game runs in pseudo-locale with no untranslated string |
| S56 | **Art integration**: the pipeline fed with real sheets as they arrive; palette and lighting pass; the CRT and glow polish; screenshots for the store | Every race and family has a sheet or a documented placeholder |
| S57 | **Audio integration**: recorded SFX and composed music behind the same events; buses; a music state machine per act | No synthesised placeholder remains on a shipping path |
| S58 | **Steam**: GodotSteam binaries, app id, achievements verified live, cloud saves, Steam Input, the demo depot with `--demo`, Workshop upload of a mod folder | The demo and the full game both on a Steam client |
| S59 | **Apple**: notarization, the universal binary, Gatekeeper clean | A macOS stranger opens the app without a warning |
| S60 | **1.0 close**: the campaign test green at every difficulty, gaps triage, `v1.0.0`, the store page live | Launch |

## Running assumptions

- **Playtesting first.** Nothing in S48–S50 is authored before S47's findings; the harness bands are hypotheses until then.
- **Content is complete; M4 does not add beats.** Writing lands as line replacements through the same data; the reactivity test and the validator keep the criteria met.
- **Placeholders ship until replaced**, with the title-screen note, as in every build since `v0.2.0-demo`.
- **The campaign test is the release gate**: no tag without it green on every path in both modes, at every difficulty since S50 (Story-Protected on Story, Mortal on Balanced, Iron Weave on Tactician).

## Progress
- S47 First external playtest — kit ready, playtest pending people: `-- --playtest` writes a log of what the harness measures (maps, fights, runs, choices, the ending); `tools/playtest_report.gd` turns a folder of logs into the triage table beside the harness bands; `docs/playtest.md` has the script, the questions and the triage template with owners. The harness bands move when two people have played. D-102.
- S48 Balance pass 1, fights — done without the playtest (the user's call): lab rounds as harness overrides, then landed: a lighter depth curve (0.10), a softer boss (2.5) with a wind-up round, a breather after a win (0.2), medkits in every Shard pool, depth adds at most one group, the Cathedral and the Approach lighter, the Choir acting second with a weaker bolt and one wisp. Harness: Undercity d3 45%, Cathedral d3 40%, Approach 30%, Markets 30% (under the 40% aim; the long cantor fights are hostile to naive play by design), the Voice 35%, every row in band. D-103.
- S49 Balance pass 2, economy and approval — done by measurement: two labs (the Act 1 walk with real fights; the naive Shard policy with the playtest log) put the road at 14 salvage and 3 Aether, a depth-1 extraction at ~20/4/52 XP, a depth-3 one at ~41/10/97, level 6 around depth 3 and level 11 around the Loom, and Sera at 8 and Kaj-7 at 9 by the Keys on the Lattice walk with the kinder choices. Changed: the Beacon's first step 15 → 12 salvage so the road pays for it; the campaign test now asserts Sera's 4 by the loyalty and 5 by the Keys on every path instead of setting them; the playtest report's RUNS table shows XP and kills. Costs, loot and the XP curve otherwise stand. D-104.
- S50 Difficulty settings and Iron Weave — done: a `difficulties` content kind (Story, Balanced, Tactician; `rules` overlays written over `rules/combat`, progression and loot for the story; saved with it; the title asks the difficulty before the death-stakes; the validator refuses a field the rules do not have); Iron Weave as a third death-stakes row: Mortal, the autosave the only save, no slot saves or loads, Continue the one way back in, a wipe or a dead leader ends the run and its save, any other new game gives it up, all carried by the account (`iron_active`, `iron_runs`, `iron_weave:<ending>` keys) and an achievement; the campaign test plays each mode on its own difficulty plus an Iron Weave Lattice run, nine endings in ~21 s. D-105.
- S51 Creator as a screen — done: New game runs title → difficulty → death-stakes → creator → plaza, with Esc backing out before anything begins and Enter starting the game with the sheet; `rules/appearance` tones and accents as two creator rows saved on the sheet, painted by the placeholder rig (head tone, hair-cap accent) and offered to sprite sheets through `skin`/`accent` palette roles; a 64×64 placeholder portrait on the same rig; locked origins listed with their unlock blurb; every attribute row previews what its next point buys. D-106.
- S52 Dialogue and journal surfaces — done: portraits from the rig for every speaker with a body (the voice's face when the speaker has none) in a portrait column on the dialogue panel; a saved history (`narrative.log`, 120 lines: lines said, choices made, quests started or moved) shown as the last three lines of the open talk above the current one and as the journal's History; a tracked quest on the HUD's fourth line, moved with ←→ in the journal and saved; the Roster and the Quarters as one `RosterMenu` screen (walking, waiting, fallen; approval, loyalty, romance; a face per row; scenes under their companion). D-107.
- S53 Pad and Deck — done: a tilted stick walks at its tilt and the combat cursor glides at the tilt's pace after its first step; rumble (`Rumble` pulses: hit, heavy, downed, boss wind-up, victory, wipe, extract) behind a setting; text size as a setting (normal/large/larger/Deck) applied to every screen-space label from its designed size, defaulting to Deck and fullscreen on a 1280×800 Linux screen or `--deck`; `--perf` puts fps and frame time on the status line; `rules/performance` and `tools/perf_budget.gd` measure every handcrafted map and the largest Shard of every biome at depth 3 (cells, nodes, enemies, pickups, logic ms) with a unit test holding the counts; the one real cost found, door scans every frame in `adjacent_door` and `check_secrets` (~6 ms in a Shard), is cached in MapData (~0.3 ms). 60 fps on a Deck itself is unmeasured: no Deck here. D-108.
- S54 Saves and settings — done: saves carry a title (leader · map · act · level) and a thumbnail beside the file when a frame exists; a saves screen (save page: the slots; load page: slots, autosave and its older copies) with the picture of the row under the cursor, a question before a slot is overwritten, opened from the pause menu and the title's Load; autosaves rotate three deep (Iron Weave keeps one); Music and SFX buses with their own volumes under the master; a colour-blind palette as one full-screen daltonizing pass (protanopia, deuteranopia, tritanopia); the settings screen grouped into Display, Audio, Pad and Bindings with a hint per row. D-109.
- S55 Localization scaffolding — done: one table (`Loc`: UI by English source, content by kind.id.path, a fallback by source text), languages as `locales` content entries a mod can ship and a Language setting that lists them, `Loc.TEXT_FIELDS` naming every content kind's text fields, `tools/extract_strings.gd` writing the full template, a `pseudo` locale that accents every string that came through, and `tests/unit/test_localization.gd` rendering every screen in it and failing on the first plain letter; some four hundred code strings and every content read the screens make now go through the table. No language has a table yet. D-110.
- S56 Art integration — done without art: every race and enemy names its sheet or says `"art": {"placeholder": "rig"}` (validator-checked; `tools/art_status.gd` writes `docs/art-status.md`, a CI step, a unit test holds it); a `LightingRig` (ambient CanvasModulate from the palette or `rules/lighting`, a PointLight2D per tile with a `glow` role up to a budget, one on the leader); a `ScreenFx` pass (glow by default, CRT, off) and Lighting as settings with `--fx=` and `--no-lighting` for screenshots; the screenshot job renders the store page's road, dialogue, creator, CRT and flat shots. D-111.
