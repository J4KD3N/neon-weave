# Decisions log

Append-only. One entry per decision the GDD does not settle. Newest at the
bottom. Format: id, date, decision, alternatives considered, why, revisit-when.

---

## D-001 — Engine version: Godot 4.6 stable, GDScript only
**Date**: 2026-09-08
**Decision**: Target Godot 4.6-stable. `project.godot` declares the `4.6` feature. CI pins the same version.
**Alternatives**: 4.3 LTS-ish behaviour; tracking latest.
**Why**: 4.6 is what the author's other Godot project already runs, so one editor serves both. GDScript static typing, typed for-loops, and `--import` headless all exist here.
**Revisit when**: a 4.7 feature is needed for a system, or a 4.6 bug blocks exports.

## D-002 — Static typing enforced by the compiler
**Date**: 2026-09-08
**Decision**: `debug/gdscript/warnings/untyped_declaration = 2` (error). Unsafe access/cast/call warnings on at warn level.
**Why**: GDD §4 says static typing everywhere. Making it an error means CI catches it via the compile-all-scripts test instead of code review.
**Revisit when**: never, ideally. Promote unsafe warnings to errors once the codebase is large enough to judge the noise.

## D-003 — Renderer: Forward+
**Date**: 2026-09-08
**Decision**: Forward+ renderer.
**Alternatives**: Mobile; Compatibility (OpenGL).
**Why**: 2D lighting and WorldEnvironment glow (GDD §5) are best supported here, and all three desktop targets have Vulkan/Metal (via MoltenVK) drivers.
**Revisit when**: Steam Deck performance profiling (M4) or macOS glow issues suggest Compatibility. Switching is a project setting, not a code change, as long as no Forward+-only shader features are used.

## D-004 — Content format and layout
**Date**: 2026-09-08
**Decision**: Content is JSON, one entry per file, at `content/<kind>/<id>.json`. `id` = file stem unless the file sets `"id"`. Kinds are folder names. Every loaded entry gets `_kind`, `_source`, `_path` injected for provenance.
**Alternatives**: `.tres` Resources (typed, editor-friendly, but unfriendly to modders and diffs); one big file per kind.
**Why**: JSON is diff-friendly, mod-friendly, and needs no engine to author. Resources can be layered later for editor tooling by loading them into the same registry.
**Revisit when**: a kind needs editor-side authoring (e.g. dialogue graphs) — add a `.tres` loader alongside JSON, not instead of it.

## D-005 — Mod discovery and override precedence
**Date**: 2026-09-08
**Decision**: A mod is a folder with `mod.json` (`id`, `name`, `version`, `priority`) and a `content/` tree with the base layout. Roots scanned in order: `res://mods` (dev), `user://mods`, `<executable dir>/mods` (exported builds only). Mods load after base in ascending `priority`, ties broken by id; last write per (kind, id) wins, so the highest priority mod overrides. Duplicate mod ids are rejected after the first.
**Alternatives**: explicit load-order file; dependency resolution.
**Why**: Simple and predictable for M0–M2. Dependencies/conflicts UI can layer on top later.
**Revisit when**: mods need to depend on other mods, or Steam Workshop lands.

## D-006 — In-repo test harness instead of gdUnit4/GUT
**Date**: 2026-09-08
**Decision**: `tests/test_runner.gd` + `tests/test_case.gd`, ~150 lines, no addon dependency. Run with `godot --headless --path . -s tests/test_runner.gd`.
**Alternatives**: gdUnit4, GUT.
**Why**: Zero setup for contributors and CI; nothing to pin. Assertions cover what M0 needs.
**Revisit when**: we want parameterised tests, scene tests with input simulation, or JUnit output for CI annotations. Migration is mechanical.

## D-007 — Platform services behind `Platform` autoload
**Date**: 2026-09-08
**Decision**: `PlatformService` autoload with a `PlatformBackend` interface. Only `NullPlatformBackend` exists now. GodotSteam (GDExtension) is added at M2 as `SteamPlatformBackend`, selected when the `Steam` singleton exists.
**Why**: GDD §4: the open repo must run Steam-free.
**Revisit when**: M2.

## D-008 — Autoload naming
**Date**: 2026-09-08
**Decision**: Autoloads are `Content` (ContentRegistry) and `Platform` (PlatformService). Autoload names must differ from `class_name`s (Godot rejects the clash), and `Steam` is reserved for GodotSteam's own singleton.

## D-009 — Base resolution and pixel sampling
**Date**: 2026-09-08
**Decision**: 1920×1080 viewport, `canvas_items` stretch, `expand` aspect, nearest-neighbour default texture filter.
**Revisit when**: the isometric camera/tile system lands (M0) and integer-scaling needs are known.

## D-010 — Licenses
**Date**: 2026-09-08
**Decision**: Code (`src/`, `tests/`, `.github/`, `project.godot`) under MIT. Content and art (`content/`, `assets/`, `docs/` story material) under CC-BY-SA 4.0. See `LICENSE` and `LICENSE-CONTENT.md`.

## D-011 — CI shape
**Date**: 2026-09-08
**Decision**: `ci.yml` on PR/push-to-main: download Godot 4.6 Linux, headless import, run unit tests, Trivy secret+misconfig scan, SBOM. `release.yml` on `v*` tags: install export templates, export Windows/Linux/macOS, attach to a GitHub Release. `security-scan.yml` weekly rescan. Adapted from the author's dev-workflow standard; commitlint step dropped because there is no Node toolchain in this repo.
**Revisit when**: first tag proves or breaks the release job (see gaps).

## D-012 — Session scope: S0 = scaffolding + content registry
**Date**: 2026-09-08
**Decision**: This first session delivered repo scaffolding, the content registry (the architectural spine for moddability), the platform abstraction, tests, docs, and CI. No gameplay. Next session: M0 isometric map + party movement.

## D-013 — Session S1 scope: M0 isometric map + party movement
**Date**: 2026-09-08
**Decision**: One handcrafted map rendered through `TileMapLayer`, click-to-move pathfinding, WASD steering, a four-member party with trail-following. No combat, no procgen, no elevation, no real art.

## D-014 — Projection delegated to TileMapLayer
**Date**: 2026-09-08
**Decision**: The `TileSet` is isometric, diamond-down, 64×32. All cell<->world conversions go through `TileMapLayer.map_to_local` / `local_to_map` (wrapped by `MapView`); the game keeps no parallel projection math. Pathfinding runs in cell space (`AStarGrid2D`), so it is projection-independent.
**Why**: One source of truth; the engine's conversions are already tested. `tests/unit/test_iso_projection.gd` pins the axis directions so a layout change cannot slip by.

## D-015 — Placeholder art is generated at runtime from data
**Date**: 2026-09-08
**Decision**: Tiles carry an `art` block (`floor` or `block` shape, palette roles); `PlaceholderTiles` draws 64×64 regions into an atlas at startup using the biome palette. Party members are tinted capsules from `PlaceholderActorArt`.
**Why**: No art pipeline yet (M1). Keeping the palette-per-biome and role indirection in data means real art slots into the same entries. The `art` block is the contract, the drawing code is disposable.
**Revisit when**: M1 art pipeline. Replace `PlaceholderTiles.build` with an atlas loader; keep the entry shape.

## D-016 — Y-sort strategy
**Date**: 2026-09-08
**Decision**: `Scene` is Y-sorted and contains `MapView` (Y-sorted; `Ground` layer at z_index −1, `Walls` layer Y-sorted) and `Party` (Y-sorted). Wall tiles and actors interleave by y; ground is forced beneath everything by z_index.
**Revisit when**: elevation or multi-storey maps arrive; those need `y_sort_origin` per tile or separate layers per level.

## D-017 — Movement model
**Date**: 2026-09-08
**Decision**: Leader: click-to-move along an A* cell path (diagonals allowed, no corner-cutting) or direct WASD steering with axis-sliding against walls. Followers: target points a fixed path-distance behind the leader on a breadcrumb trail (`FormationTrail`), so they only ever walk where the leader walked and never need their own collision. Speed 170 px/s, spacing 36 px, both exported on `Party`.
**Alternatives**: NavigationAgent2D + avoidance; formation slots with per-follower pathing.
**Why**: Deterministic, testable without physics, and cannot desync followers into walls. Good enough until combat needs per-member positioning (which is turn-based and grid-snapped anyway).
**Revisit when**: real-time exploration needs followers to route around each other, or free-form formations are wanted.

## D-018 — Input actions registered in code
**Date**: 2026-09-08
**Decision**: `InputActions.ensure()` adds actions to `InputMap` at scene start instead of an `[input]` block in `project.godot`.
**Why**: Diffable, headless-safe, and settings UI can rebind at runtime later. Avoids hand-editing serialized `InputEventKey` blobs.

## D-019 — CI screenshot job
**Date**: 2026-09-08
**Decision**: `ci.yml` gains a best-effort `screenshot` job: Xvfb + Mesa software GL, `--rendering-method gl_compatibility`, the scene saves the viewport after ten frames via `-- --screenshot=<path>` and quits; the PNG is an artifact. Not a required check.
**Why**: No local Godot on the build machine. This is the only visual verification loop until the editor is opened.

## D-020 — Runtime script errors fail CI; tests run in the first frame
**Date**: 2026-09-08
**Decision**: The runner executes tests from the first `_process` frame, not `_initialize`, because the root window is not in the tree during initialisation and scene nodes added then never get `_ready`. Since a GDScript runtime error aborts a method without recording a failure, the CI test step also fails on any `SCRIPT ERROR` in the log.
**Why**: Discovered in S1: the scene tests "passed" while every line after the first error was skipped.
**Revisit when**: moving to gdUnit4/GUT (D-006), which handle both.

## D-021 — Session S2 scope: enemies + turn-based combat
**Date**: 2026-09-08
**Decision**: Three Rusted Undercity enemies, encounter triggers, a pure turn-based engine, rusher/ranged AI, a code-built HUD. Deferred: cover, elevation, surfaces, class resources (Surge/Vent Heat), stealth openers, XP/loot, extraction.

## D-022 — Combat engine is pure and event-driven
**Date**: 2026-09-08
**Decision**: `CombatState` owns all rules over `MapData` + `Combatant` data + ability entries and emits `event` dictionaries; `CombatController` (a Node) maps combatant ids to `WorldActor`s, animates, and drives the HUD. With `animate = false` a whole fight resolves synchronously, which is how the scene test and the CI combat screenshot run.
**Why**: Unit tests for every rule without nodes or timing; AI can be tested against the same object; determinism via a seeded `RandomNumberGenerator`.

## D-023 — Turn economy and range metric
**Date**: 2026-09-08
**Decision**: Per turn: `ap_per_turn` (4) action points for abilities plus `move` cells of free movement. Reach is a BFS over walkable, unoccupied cells, diagonals allowed, no corner cutting (consistent with exploration pathing). Range and adjacency use Chebyshev distance. Line of sight is a Bresenham walk; only tiles with `blocks_sight` (walls) break it; low debris and combatants do not.
**Alternatives**: AP-for-movement (XCOM style); Euclidean ranges.
**Why**: GDD §9 says free movement + 4 AP. Chebyshev keeps melee 8-directional and matches the diagonal BFS.

## D-024 — To-hit, flanking, friendly fire, knock-outs
**Date**: 2026-09-08
**Decision**: hit chance = ability `accuracy` − target `evasion` (+ `flank_hit_bonus` when another combatant hostile to the target is adjacent), clamped to [5, 95]; damage is uniform in `[min, max]`, ×`flank_damage_mult` when flanked. Abilities may target anyone but self (friendly fire on, rule-toggleable). Party members at 0 HP are `downed` under Story-Protected rules (default) and revived at 1 HP after a victory; Mortal mode kills. All numbers live in `content/rules/combat.json`.
**Why**: Enough tactical texture (positioning matters via flanking and LOS) for M0's "is it fun?" without cover/elevation yet. Story-Protected vs Mortal is the GDD §7 setup choice; only the rule flag exists so far, no setup UI.

## D-025 — Encounters
**Date**: 2026-09-08
**Decision**: An enemy that has a party member within its `awareness` (Chebyshev) with line of sight starts combat. Clicking an enemy within the leader's reach of its awareness starts combat with a first-strike initiative bonus for the party. Only enemies within `engage_radius` (7) of any party member join; the rest idle for later. Party members snap to distinct free cells (`CellSettler`) at combat start.
**Revisit when**: stealth (GDD §9) lands — awareness should become a cone/noise model, and first strike should be a proper opener.

## D-026 — Enemy AI is per-action, not per-turn
**Date**: 2026-09-08
**Decision**: `EnemyBrain.next_action(state, actor)` returns one action (move / ability / end); the controller executes it, animates, and asks again. Archetypes: `rusher` (close to nearest hostile, use highest-damage usable ability) and `ranged` (kite to distance 2..range with LOS, then shoot). Unknown archetypes fall back to rusher.
**Why**: Each decision sees the true state after the previous action, and the view gets natural animation beats. Summoner/stealther/controller wait on their mechanics.

## D-027 — Session S3 scope: procgen Shard biome
**Date**: 2026-09-08
**Decision**: One generator (`ShardGenerator`) + one validator (`ShardValidator`) + one template (`content/shards/rusted_undercity.json`). Debug keys N (new Shard) and H (home) stand in for the Bastion's Beacon until it exists. Deferred: secrets, vaults, merchants, biome variety, the extraction loop itself (next session).

## D-028 — Generated Shards are map entries
**Date**: 2026-09-08
**Decision**: The generator emits a dictionary in the exact shape of `content/maps/*.json` (rows, legend, spawn marker, enemy placements) plus `extraction`, `rooms` and `generation` metadata. The world loads both through the same `load_map_entry`; MapData, MapView, NavGrid, encounters and combat do not know whether a map was handcrafted.
**Why**: One code path, one validator for both, and modders can author templates or hand maps with the same vocabulary.

## D-029 — Solvability by construction plus an independent validator
**Date**: 2026-09-08
**Decision**: Rooms and L-corridors connect every room in placement order (plus random loops). Debris is only placed on "simple points": cells whose walkable ring neighbours stay 4-connected through the ring, so removing the cell cannot split the map. A final 4-connected flood from the spawn walls off anything unreachable. The validator re-checks everything under the exploration movement rules (8-connected, no corner cutting): closed border, 4 spawns, extraction reachable, every walkable cell reachable, enemies reachable/distinct/off spawn and exit/at least `min_spawn_distance` away. `tests/unit/test_shard_generator.gd` sweeps 80 seeds and a heavy-debris variant.
**Why**: GDD §11 "guaranteed-solvable" and §4 "unit tests for procgen validity". The generator's local rule keeps generation O(cells); the validator is the safety net and also runs at load with warnings.

## D-030 — Shard template vocabulary
**Date**: 2026-09-08
**Decision**: Templates carry `size`, `rooms` (count/min/max/attempts), `corridors.extra_loops`, `tiles` roles (wall/floor/grate/debris/extraction), `grate_patch_chance`, `debris_density`, and `enemies` (groups, group_size, min_spawn_distance, weighted pool). Ranges are `[min, max]` inclusive and rolled from the seed.
**Revisit when**: a second biome needs different room shapes (caverns, datacore halls); add a `style` switch rather than a second generator.

## D-031 — Session S4 scope: the extraction loop
**Date**: 2026-09-08
**Decision**: Run haul + persistent ledger, loot on kills, pickups in Shards and on the yard, extraction on the pad, wipe = haul lost. This completes the M0 loop: yard → Shard → fight/collect → extract or wipe → yard. Deferred: XP spending/levels, Bastion buildings, merchants, item loot, the death-stakes setup UI.

## D-032 — Haul vs ledger
**Date**: 2026-09-08
**Decision**: `RunState` holds the unbanked haul (Salvage, Aether, Ciphers, XP, kills) for one expedition; `Ledger` holds banked totals and lifetime counts and is the only thing saved. Extraction banks and records a run; a wipe inside a Shard clears the haul, records the wipe, and still counts kills. On handcrafted maps (home) there is no extraction risk, so gains bank immediately.
**Why**: GDD §11: "unbanked resources lost on wipe, story progress never lost". Keeping the two objects separate makes the rule a data flow rather than a set of flags.

## D-033 — Grant blocks are the loot vocabulary
**Date**: 2026-09-08
**Decision**: Enemies carry `loot`, pickups carry `grants`; both use the same block: a number or `[min, max]` per resource, `cipher_chance` for a bonus Cipher, `xp`. Rolled by `RunState` with an RNG seeded from the Shard id and seed, so a seed reproduces its loot. No item drops yet; rarity/affixes (GDD §12) come with the item system.

## D-034 — Ledger persistence
**Date**: 2026-09-08
**Decision**: `user://ledger.json`, `{"version": 1, ...}`, saved on every bank/wipe. `Ledger.migrate` is the migration hook (a pre-versioned flat shape is upgraded as the first example). Corrupt files are reported and ignored, never overwritten until the next save. Steam Cloud sync of this file is an M2 concern behind `Platform`.
**Revisit when**: the full save system (slots, autosaves, party state) lands; the ledger becomes one section of it.

## D-035 — M1 is planned as sessions S5–S12
**Date**: 2026-09-08
**Decision**: `docs/m1-plan.md` fixes the order: Bastion → save/load → full combat → classes 3–4 → creator → art pipeline → Sera → controller. Each is one session/PR. The yard stays the Bastion's grounds until the art/companion sessions decide on a dedicated map.

## D-036 — Bastion buildings are levelled content entries
**Date**: 2026-09-08
**Decision**: `content/buildings/<id>.json` with `levels[]`; level 0 is free and always built; upgrading to level n pays `levels[n].cost` from the ledger. Effects are flat keys summed across buildings' current levels (`depth`, `heal_fraction`, `hp_bonus`, `damage_bonus`); the world applies them. Levels persist in the ledger (v2).
**Why**: GDD §12 wants buildings that visibly upgrade; keeping effects as summed keys means a new building is a JSON file and possibly one new key handled in the world, never a new class.
**Revisit when**: buildings need choices per level (branching upgrades) or visuals on the map.

## D-037 — Party HP persists between runs; the Med-bay is the heal
**Date**: 2026-09-08
**Decision**: The party is spawned once per scene; entering any map repositions the same nodes. Arriving home applies the Med-bay's `heal_fraction` of max HP (minimum 1) and revives the downed. Victory still leaves the downed at 1 HP. Workshop `hp_bonus` raises max HP and current HP by the same amount.
**Why**: Without persistence the Med-bay is meaningless and wounds have no weight; with it, "extract now or push on" becomes a real decision.

## D-038 — Beacon depth is the only difficulty knob for now
**Date**: 2026-09-08
**Decision**: `ShardGenerator.generate(template, seed, depth)` adds `depth-1` enemy groups and pickups; layout is unchanged for a given seed. Enemy stats do not scale yet.
**Revisit when**: S7 (full combat) or the balance pass wants scaling stats, elites, or per-depth pools.

## D-039 — Save model: deltas over deterministic content
**Date**: 2026-09-08
**Decision**: A save (`SaveSystem`, JSON, `version`) stores the ledger (which carries Bastion levels), the party's HP/downed/cells, the run haul, the location, and deltas against the loaded map: indices of dead enemies and collected pickups. A Shard location is `{template, seed, depth}` and regenerates on load; a handcrafted location is a map id. Loading applies the save's ledger and rewrites `user://ledger.json`, so the books never fork.
**Alternatives**: serialising every node; storing tile rows for Shards.
**Why**: Small files, no drift between save shape and scene shape, and procgen determinism is already tested. Placement indices are stable because both maps and the generator emit ordered arrays.
**Revisit when**: enemies move or spawn dynamically (indices become ids), or item inventories arrive.

## D-040 — Slots and checkpoints
**Date**: 2026-09-08
**Decision**: `user://saves/slot_1..3.json` and `autosave.json`. Save anywhere out of combat (refused during a fight). Autosave at: entering a Shard, combat start (the "combat checkpoint"), extraction, and returning home after a wipe. After a wipe, Esc reloads the checkpoint, R goes home. Loads suppress autosave so a load never overwrites the checkpoint it reads. Keys: F5 save slot 1, F9 load slot 1, F10 load autosave; a slot picker UI is not in M1.
**Why**: GDD §13 verbatim, minus Steam Cloud (M2, behind `Platform`).

## D-041 — Session S7 scope: full combat texture
**Date**: 2026-09-08
**Decision**: Cover, one storey of elevation, three surfaces and the two prototype class resources, all as tile/ability/resource data read by `CombatState`. Deferred: hackable turrets/doors, difficulty settings, Iron Weave, AI awareness of surfaces, movement cost for elevation.

## D-042 — Cover and elevation
**Date**: 2026-09-08
**Decision**: A tile with `cover: n` (debris) shields a target from ranged fire when it sits on the neighbouring cell in the attacker's direction (sign of the delta): −`cover_hit_penalty`. Melee ignores cover; attackers on higher ground ignore it. `height: 1` tiles (catwalk) give +`elevation_hit_bonus` and +`elevation_damage_bonus` when attacking down and −hit when attacking up. Movement between storeys is free (no stairs rule) so procgen validity is untouched.
**Why**: Cheap, legible, and both answer "does where I stand matter?" without a physics query.
**Revisit when**: real art needs visible ramps, or AI should seek cover/high ground.

## D-043 — Surfaces
**Date**: 2026-09-08
**Decision**: `surface` on a tile: `mana_pool` (arcane casts from it ×`mana_pool_amplify`), `conduit` (a tech/arcane hit on someone standing on it arcs `conduit_chain_damage` to every other occupant of the 4-connected conduit run, allies included), `corrosive` (`corrosive_damage` at turn start; can down or kill). Shard templates carve surface patches with their own legend chars; the yard has samples.
**Why**: GDD §9 names these three; each is one lookup in the engine and one field in data.

## D-044 — Class resources are `resources` entries
**Date**: 2026-09-08
**Decision**: `content/resources/<id>.json`: `builds_on` damage type, `max`, `gain_per_cast`, `damage_per_stack`, optional overload (`overload_at`, `overload_chance`, `overload_damage`: the cast fizzles, AP is spent, the caster takes damage, stacks reset) and optional lock (`lock_ap_at_max`: abilities costing that much AP are refused at max stacks) with a `vent_ability` (`effect: "vent"`, `targets: "self"`, resets stacks, heals). Classes point at a resource by id; enemies have none. Stacks start at 0 per fight.
**Why**: Surge and Heat are the GDD's two prototype resources and both fit "build on matching casts, pay out as damage, with a downside at the cap". Hexes, Scrap Charge, Null and Wireghost Heat (S8+) should extend this vocabulary rather than add engine branches.

## D-045 — Classes 3–4 and the resource vocabulary
**Date**: 2026-09-08
**Decision**: Circuit-Witch and Drone Shepherd are content entries. Their loops needed three generic additions to the ability/resource vocabulary, added once and reusable: (1) `effect: "mark"` + `mark` stacks a named mark on a hit target up to the caster resource's `max_per_target`; `effect: "detonate"` adds `damage_per_mark` × stacks and clears them; a resource with `kind: "marks"` reads as the live total of its mark; (2) `resource_cost` on an ability is checked in `can_use` and spent on cast; (3) `gain_on_kill` and `gain_on_surface` on a resource harvest charge. Surge/Heat are unchanged. Anyone may place marks; only mark-kind resources read them.
**Why**: GDD §15 says a new class must not need engine changes. The vocabulary did need three verbs, so the honest record is: the engine grew a vocabulary, and the classes are data. Null Blade (absorb magic as fuel) and Wireghost (Heat from stealth/hacks) should be expressible with `gain_on_*` style fields plus at most one new verb.

## D-046 — Prototype party is one of each class
**Date**: 2026-09-08
**Decision**: `parties/prototype.json` fields Scrap-Knight, Aetherbinder, Drone Shepherd, Circuit-Witch. Class colours follow the first branch (Circuit-Witch reads Arcane purple, Drone Shepherd Tech teal).

## D-047 — Session S9 scope: creator v1
**Date**: 2026-09-08
**Decision**: `CharacterSheet` (data) + `CreatorState` (pure model: rows, cycling, point buy, validation, text render, stat preview) + `CreatorMenu` (text panel, name field, 4× paper-doll preview) + `PartyBuilder` (preset + optional protagonist → member specs). The protagonist replaces the preset's first member with id `protagonist`, lives in saves under `protagonist`, and is respawned on load. C opens the creator at home; there is no title screen yet.
**Why**: Everything the screen shows is derived from data (races, origins, classes, `rules/attributes`), and the model is testable without nodes.

## D-048 — Races, origins, attributes as data
**Date**: 2026-09-08
**Decision**: The five M2 races (Trueborn, Chromed, Aetherborn, Synth, Rootkin) carry `stat_mods`, `faction_lean` and an `overlay` `{kind, color}` stamped onto the shared humanoid rig (`marks`, `chrome`, `bark`, `plating`, `none`) without changing its silhouette (GDD §5: one rig, race overlays). Origins carry `dialogue_tag` (for later gated dialogue) and `stat_mods`. Attributes are `rules/attributes`: three names mirroring the Weave branches, a point budget, a per-attribute cap, and per-point stat effects applied by `StatBlock`.
**Revisit when**: real art replaces the placeholder rig (S10) — overlays become sprite layers, the entry shape stays.

## D-049 — Session S10 scope: art pipeline with generated placeholders
**Date**: 2026-09-08
**Decision**: The pipeline (sidecar contract, loader, facing/mirroring, palette swaps, movement- and combat-driven animation) ships now; the art itself is generated by `tools/gen_placeholder_sheets.js` for one race (Trueborn) and one enemy (Scav). Every other actor keeps the placeholder capsule until it gets a sheet. `docs/art-pipeline.md` is the artist-facing contract.
**Why**: The M1 exit criterion "one fully animated actor" is met with placeholders that exercise every code path real art will use, without pretending generated capsules are final art.

## D-050 — Sheets are raw PNGs read by the game, not imported textures
**Date**: 2026-09-08
**Decision**: `content/sprites/` carries a `.gdignore`; the game reads PNG bytes and builds textures itself. Export presets include `content/*.png`. Mods ship sheets under their own `content/sprites/`.
**Why**: Sidecar-driven loading needs the exact pixels (palette swaps by colour equality); the importer would compress or mipmap them. Mods and artists only touch files, never the editor.

## D-051 — Facings and animation naming
**Date**: 2026-09-08
**Decision**: Facings are screen-space names (e, s, w, n and diagonals); sheets declare which rows they draw and which facings mirror onto them. Animations build as `<animation>_<row>`; the runtime resolves any direction vector to the nearest facing. Required set: idle, walk, attack, hit, death; optional cast (arcane abilities). Colour roles fill/outline/highlight are swapped from the actor tint; biomes may override an enemy sheet's tint via `recolors: {sheet: palette role}`.
**Revisit when**: real art wants per-direction frame counts, race overlay compositing, or shader-based palette swaps.

## D-052 — Session S11 scope: Sera and the narrative data model
**Date**: 2026-09-08
**Decision**: `companions`, `dialogue`, `quests` content kinds; `NarrativeState` (flags, approval, quest stages, recruits) saved under `narrative`; a text dialogue panel; Sera recruitable in the yard; her quest reaching into Shards as a placed site; banter on triggers; approval on the HUD. Both death-stakes paths are data. Deferred: romance, dialogue portraits/voice, companion AI personalities, a quest journal UI, dialogue on the map beyond companions.

## D-053 — Dialogue format
**Date**: 2026-09-08
**Decision**: A dialogue is `{start, nodes}`. `start` is a node id or an ordered list of `{node, requires}` (first passing wins; the last must be unconditional). A node is `{speaker, text, choices}`; a choice is `{text, requires, effects, next | end}`. `requires` keys: `flags`, `origin_tag`, `race`, `class`, `approval{min,max}`, `recruited`, `not_recruited`, `quest{id,stage}`. `effects` keys: `approval`, `flags`, `recruit`, `quest`. Banter dialogues are `{kind: "banter", lines: [{trigger, text, requires, effects, once}]}`; the first matching unspent line plays. Every node must keep at least one unconditional choice so no player can dead-end (enforced by the content test).
**Why**: Small enough to hand-write and validate in CI; expressive enough for gated lines, approval, recruitment and quest stages. The `DialogueRunner` is pure and the panel only renders it.

## D-054 — Quests and Shard sites
**Date**: 2026-09-08
**Decision**: A quest is `{companion, start, stages: {id: {summary, shard_site?, complete?}}}`; the narrative stores one stage per quest. A stage with `shard_site: {pickup}` asks the generator to place that pickup once in every new Shard; a pickup with `dialogue` opens it on touch instead of granting loot. Saves keep the placed extras in the location so a reload regenerates the same layout even after the stage moves on.
**Why**: "Companion quests intersect the main plot" starts with quests intersecting the loop. Sites as pickups reuse placement, validation, deltas and saves unchanged.

## D-055 — Death-stakes in data and in the roster
**Date**: 2026-09-08
**Decision**: With `story_protected` off, a companion killed in a won fight leaves the party, is un-recruited, and sets `<id>_dead`; a dead leader is a wipe. Quest dialogues branch on `sera_dead` / `recruited` from their start list, so every companion quest ships the Story-Protected path, the never-recruited path and the posthumous path as nodes (GDD §7, §15).

## D-056 — Party of four means three preset members plus a recruit
**Date**: 2026-09-08
**Decision**: `parties/prototype.json` is three members; `rules/combat.party_max` is 4; recruits append in order until full. Moth the Circuit-Witch leaves the debug preset (the creator can still make one).

## D-057 — Turn groups: consecutive allies act in any order
**Date**: 2026-09-08
**Decision**: Initiative still sorts every combatant, but a run of same-team combatants in the order forms a *turn group*. Within the group the player swaps freely (Tab, or click an ally) until each member has ended its turn; a member begins its turn (AP and Move restored, surface effects) the first time it gets control, keeps what it spent when swapped away from, and cannot be returned to once it pressed End Turn. Enemies use the same rule one at a time. Groups never span a round boundary.
**Why**: The first playtest could not tell who was acting or why it could not pick someone else. This is the BG3 grouping the GDD points at, without abandoning initiative.

## D-058 — Previews before commitment
**Date**: 2026-09-08
**Decision**: `CombatState.preview()` computes the to-hit chance and damage span for an ability on a target with every modifier the roll would use (flanking, cover, elevation, mana pool, resource stacks, marks, Workshop) and the reason it would be refused. The controller shows it on hover, along with a path preview for moves and a hover marker; refusals go to the hint line in red with the ability and reason spelled out. The preview and the roll share one bonus function so they cannot drift.
**Why**: A turn-based game with hidden odds is a guessing game. Same rule for every future ability: if it changes the roll, it changes the preview.

## D-059 — One action set for keyboard and pad, routed by mode
**Date**: 2026-09-08
**Decision**: Gamepad events join the existing keyboard actions in `InputActions.BINDINGS` rather than forming a second set. Bindings may overlap across actions (Esc is `cancel` and `menu`; Start is `end_turn` and `menu`) because `ExploreWorld` only checks the actions its current mode uses. Mouse-only paths get pad equivalents: a cell cursor in combat (stick/D-pad/WASD, Enter/A confirms, mouse motion releases it), Enter/A as "interact" in exploration (extract on the pad, talk to a companion within two cells), and cursors in the dialogue and Bastion menus. Every keyboard-only key (N, H, E, B, C, F1, F5, F9, F10) is an item of the system menu on Start/Esc; `PAD_ROUTES` documents each route and a test enforces that every action is pad-bound or routed.
**Why**: "Every input path has a pad binding" is the M1 exit criterion. A separate pad action set would double every routing branch; overlapping bindings cost nothing when routing is already per mode.

## D-060 — Steam Deck layout is the default pad layout
**Date**: 2026-09-08
**Decision**: The default pad layout is designed for the Deck first: left stick and D-pad move/cursor, A confirm, B cancel, X/Y/L1/R1 abilities 1–4, L2/R2 zoom, Start system menu / end turn, Select swap member. No Steam Input template ships until GodotSteam lands (M2); the layout above is what that template will mirror. Documented in `docs/controller.md`.
**Why**: The Deck is the reference pad device for the Steam target (GDD §3). Designing for its buttons first keeps the layout inside what every XInput pad also has.

## D-061 — Engine version moves to Godot 4.7.2 (supersedes D-001)
**Date**: 2026-09-08
**Decision**: Target Godot 4.7.2-stable. `project.godot` declares the `4.7` feature; `ci.yml` and `release.yml` download 4.7.2-stable and its export templates. No code changed: 4.7.2 ran the 4.6 project and all 240 tests unmodified.
**Why**: The first playtest ran on the 4.7.2 editor, which rewrites `project.godot` (feature tag, comment stripping) on every open. Pinning CI and the editor to one version stops that churn and keeps "never break exports" checkable locally.
**Revisit when**: a 4.8 feature is needed, or a 4.7 export bug blocks a release.

## D-062 — M2 is three tracks: systems, content, then art and store
**Date**: 2026-09-08
**Decision**: `docs/m2-plan.md` schedules fifteen sessions (S13 playtest triage → S27 demo tuning) in dependency order: progression and the last two classes before the enemy AI and second biome; campaign tooling before companions, the Lattice and the Act 1 slice; front end and audio before Steam; release before tuning. Art and the store page are parallel tracks with an explicit decision point at S22 on shipping the demo with placeholder art. The demo slice ends before first contact with the Choir fragment; the level cap is 6 in the demo; Dax ships as a companion-only Vaultkin entry.
**Why**: M1 proved the systems are data; M2 is judged by strangers on Steam. Keeping content sessions after their tooling avoids authoring twice, and naming art as a non-code gate keeps every session unblocked until the last possible moment.

## D-063 — S13 triage ships a soak test alongside the fixes
**Date**: 2026-09-08
**Decision**: `tests/unit/test_soak.gd` plays three seeded runs of the whole loop headless (yard, Shard, fights with swaps/undo/aiming, mid-run save and load, extraction or wipe, Bastion upgrades, Sera) and checks invariants after every action. It runs on every PR like any other test. The playtest items the plan named are in: the camera follows whoever is acting in combat and returns to the leader; Esc/B with nothing aimed undoes the last move (until any ability, swap or turn end); the system menu lists all three save slots and the autosave with their summaries.
**Why**: The author is the only playtester and cannot be on call for every PR. A scripted playtester finds the crashes; a human finds the feel. Both are S13.

## D-064 — Party-wide level from banked XP; builds live in the ledger
**Date**: 2026-09-08
**Decision**: XP is party-wide (it was already banked on extraction into the ledger), so the party has one level, read from `rules/progression.xp_curve` with `level_cap` (6 for the demo). Per-class `growth` scales stats per level and `unlocks` adds abilities by level. At `subclass_level` each member picks one of the two `subclasses` entries of its class (abilities, stat mods, damage bonus). Talents (`talents` kind: branch, tier, Aether cost, effects, requires) are bought per member with banked Aether; tiers open by level. Per-member choices persist as `Ledger.builds` (ledger v3), so they survive wipes like everything banked and ride along in saves with the ledger. The Arcanum is the fourth Bastion building: level 1 allows respec with a 50% Aether refund, level 2 refunds in full. Progression math is pure (`Progression`), applied by `PartyBuilder`, and re-derived in place on level-up or a Weave change so HP shifts like a Workshop upgrade instead of respawning the party.
**Why**: Per-character XP in a party game punishes swapping members and adds bookkeeping the GDD never asks for. Keeping builds in the ledger means one migration path and no new save section.
**Revisit when**: multiclassing (level 5 in the GDD) or per-character levels are needed; the Weave Tree needs more than flat stat talents.

## D-065 — Stealth, ambush, absorption and silence as resource/ability vocabulary
**Date**: 2026-09-08
**Decision**: Four generic mechanics carry the last two classes. `effect: "stealth"` (self) sets `hidden`: hostile abilities and the enemy brain cannot target a hidden combatant; attacking from hiding is an ambush (`rules.ambush_hit_bonus`, `ambush_damage_mult`) and reveals; any damage taken reveals. A resource may declare `gain_on_stealth` and `reveal_at_max` (Wireghost Heat: hiding heats, the cap lights you up until the `vent_ability` cools you). A resource may declare `absorb: {type, fraction}` and `gain_per_absorbed` (Null Blade: half of arcane damage, chain shocks included, becomes Null for the lash). `effect: "silence"` with `duration` puts `statuses.silenced` on the target; arcane abilities are refused while it lasts and it counts down at the target's turn start. Abilities may declare `cooldown` (turns, counted down at the user's turn start; Shadow Step: 3) so a stealther is exposed for a turn between windows and cannot stall a fight by re-hiding forever. The `stealther` archetype hides when nothing is in reach, closes in unseen and ambushes; the Wire-Lurker uses it in the Undercity pool. Scrap-Knight Heat is renamed Vent Heat so the two resources read apart.
**Why**: GDD §8 gives Wireghost and Null Blade identities that need engine support; naming the mechanics generically means Razor/Phantom/Voidfencer/Suppressor and future enemies get them for free (GDD §15).

## D-066 — Enemies approach by walking distance, not straight-line distance
**Date**: 2026-09-08
**Decision**: `CombatState.distance_field(goal)` is a BFS over walkable cells (movement adjacency, occupants ignored); `EnemyBrain._closest_reachable` picks the reachable cell with the smallest walking distance, falling back to straight-line only when the goal is cut off. The soak test policy uses the same field. `tests/test_runner.gd` accepts `-- --only=<fragment>` to run one file.
**Why**: The soak test stalled a fight at round 366: a scav and the last party member stood three cells apart with a wall between them, and the greedy approach could not find the way round. A stealth window and cooldown (D-065) stop hidden enemies from stalling; this stops visible ones. Full AI awareness of cover, elevation and surfaces is still S16.

## D-067 — Enemy AI v2: five archetypes over one texture score
**Date**: 2026-09-08
**Decision**: `EnemyBrain.position_score` rates a cell against the target: cover between the two (`ai_cover_weight`), high ground (`ai_elevation_weight`, negative when below), a mana pool for arcane casters (`ai_mana_pool_weight`), corrosive biogrowth (`ai_corrosive_penalty`). Rushers approach by walking distance with texture breaking ties; ranged enemies fire from any cell in the kite band unless a reachable cell has strictly better texture, then relocate first; stealthers hide, close and ambush; summoners call minions to their `summon_max` then fight as ranged; controllers land fresh control effects (root, silence) on the nearest unaffected target then fight as ranged. Control effects are never re-applied to a target that already has them. New vocabulary: `effect: "summon"` (`summon`, `summon_max`, `cooldown`; minions get `summoned_by`, join the order and the world as real enemies with loot and XP), `effect: "root"` (no movement for `duration` own turns). Statuses now count down when the affected combatant's turn ends, so "silenced 2" is two silenced turns.
**Why**: S7 built cover, elevation and surfaces that only the player used. One scoring function, weighted from `rules/combat`, gives every archetype the same awareness and keeps the archetypes themselves small. Each archetype has a scripted fight it wins against a party that only swings back.

## D-068 — Elites, bosses and depth scaling (closes D-038)
**Date**: 2026-09-08
**Decision**: `StatBlock.for_enemy(entry, rules, depth, tier)`: elite x1.5 HP +1 damage; boss x3 HP +2 damage +1 AP; every enemy gets HP x (1 + 0.15 x (depth - 1)) and +1 damage per two depths past the first. A placement may carry `tier`; an entry may declare its own (the Undercity Warlord is a boss). The generator rolls elites per placement (`elite_chance_per_depth`, capped) and, from `boss.min_depth`, posts the template boss on the floor cell nearest the extraction pad. Depth 3 is measurably harder than depth 1 in the scene test: more bodies, more than 1.3x the total HP, and a boss on the pad.
**Why**: D-038 said depth only adds bodies. The demo needs a difficulty ramp the Beacon controls, and a boss at the pad turns extraction into a decision.

## D-069 — Biomes are templates plus a palette; the Beacon unlocks them
**Date**: 2026-09-08
**Decision**: A second biome is a `biomes` palette (same sixteen roles), its own tiles (moss floor, root wall, sap pool, data vine, rootrot, canopy, spore bed) and a `shards` template that names them, its enemy family and its boss. Templates can ask for `rooms.style: "oval"` (ellipse hollows) and `corridors.width` (1–3). A template with `requires_unlock` is launchable only when a reached Bastion level lists that id under `unlocks` (Beacon level 2 finds the Verdant Datacore); the system menu lists every template and N launches the last chosen one. `--biome=<template>` before `--shard=` renders it in CI. No engine change was needed for the family: hybrids, constructs, a summoner and a controller boss reuse S15/S16 vocabulary.
**Why**: The second biome is an M2 exit criterion and the first proof that biomes are data (GDD §15). Beacon-gated unlocks make the Bastion the place where the world grows.

## D-070 — Spores: a surface that shrouds whoever stands in it
**Date**: 2026-09-08
**Decision**: `rules/combat.surface_evasion` maps a surface to evasion granted to a target standing on it (`spore`: 10). It applies in `attack_modifiers` (tag "spores" in previews and the log) and the enemy brain values such cells at half the bonus. Rootrot, sap pools and data vines are corrosive, mana-pool and conduit tiles under new names, so the Datacore reads differently without new mechanics.
**Why**: One new surface mechanic per biome keeps the vocabulary growing slowly and the validator honest.

## D-071 — Shard features are carved data with validator rules
**Date**: 2026-09-08
**Decision**: A template's `features` block asks for secrets, vaults, waypoints, a merchant, a rarity table and a ramp fraction. The generator carves them first, after the walk graph is final, so the layout never depends on depth or quest extras: secrets are one-cell pockets behind a `secret_door` tile that gives when a party member stands beside it; vaults are two-cell pockets behind a `vault_door` that costs banked Ciphers (E / A / click) and hold loot of at least the vault rarity; relay waypoints are `waypoint` tiles on the way to the pad where E banks the haul without leaving, once each; the merchant is an NPC placement (`merchants` kind, stock paid from the banked ledger) at the halfway point. The difficulty ramp is `ramp_far_distance` in the generator's own four-way metric: groups past it are one larger and only they roll elites. Pickups carry `rarity` (common/rare/epic, `rules/loot`) which multiplies every grant and tints the gem. Opened doors and used relays are run deltas in saves. Every feature has a validator rule: pockets must be sealed until their door opens and open with it, vaults need a cost and loot, waypoints must be real tiles on the open path, the merchant on reachable floor, elites past the ramp, rarities known.
**Why**: GDD §11 lists these as what makes Shards replayable, and Ciphers finally have a sink. Carving before population keeps the "layout is seed-only" guarantee the save system relies on. Item affixes wait for an item system (M3).

## D-072 — Campaign tooling: map format v2, quest objectives, the Bastion map
**Date**: 2026-09-08
**Decision**: Handcrafted maps gain `transitions` (leader on a cell travels to another map and arrives at a named cell; optional `when` conditions), `triggers` (cells that fire once or every time, gated by `when`, with effects: the dialogue effect keys plus `toast`, `open_doors`, `enemies` that spawn and start a fight, `dialogue`, `transition`), `doors` (`locked_door` tiles opened by Interact when a `key_flag` is set, optionally setting `opens_flag`), and `buildings` (Bastion sites). Door state on handcrafted maps and spent once-triggers live in narrative flags (`door_<map>_<x>_<y>`, `trigger_<map>_<id>`), so they persist with the story rather than the run. Quest stages gain `objectives` (`text`, `done_when` conditions) and a quest may be `main` (no companion) and `auto_start`; the journal (J, or the system menu) ticks objectives against the narrative state. The home map is now `bastion`, a plaza with the four buildings standing on it as `BuildingActor` blocks that grow a storey per level; the yard is a handcrafted map through the gate, and the gate road east of it is the first authored story map: a lever trigger, a locked gate, an ambush, and the end of the road closing the main quest's first stage. `home_map` is an export so tests keep the yard as home.
**Why**: S20–S22 author companions, the Lattice and Act 1 on top of this. One story map that plays end to end in a test (lever → gate → ambush → road end → journal done → reload) proves the format before content depends on it.

## D-073 — Three companions, one protagonist: the demo party
**Date**: 2026-09-08
**Decision**: The default party preset is `demo`: the protagonist alone, with Sera (the yard), Dax (the yard gate) and Kaj-7 (the gate road, once the road is open) filling the other three slots as they are recruited, which is exactly `party_max`. Tests pin the three-member `prototype` preset. Kaj-7 (Synth Drone Shepherd, Lattice-courted) and Dax (Vaultkin Null Blade, Ashfound-leaning) are registry entries with recruit/talk/banter dialogue and a personal quest each, both quests intersecting the main plot: Kaj-7's maker-signal is a Choir thread (its recruit waits on the main quest opening the road; the relay names the Choir), Dax's seal runs on Weft-light he cannot touch. The casting rule holds on every recruit and quest choice: one companion approves, another is wounded. Vaultkin is a race entry with `playable: false`, kept off the creator (one generic filter). Both quests ship all three death-stakes paths.
**Why**: Three companions is the M2 exit criterion, and the S11 data model held: no engine change beyond the playable flag.

## D-074 — Factions are reputation plus a casting rule; joining waits for Act 2
**Date**: 2026-09-08
**Decision**: `factions` is a content kind (name, colour, `rivals`, `joinable_act`). `NarrativeState.reputation` holds a score per faction, saved with the story; conditions take `reputation: {faction: {min, max}}` and effects take `reputation: {faction: delta}`. When an effect block moves a faction's reputation, every recruited companion leaning that way (`companions.faction`) gains a point of approval and every one leaning toward a rival loses one, unless the same block set their approval explicitly. Story NPCs are a kind (`npcs`: name, faction, dialogue, art) placed on maps with `npc` and an optional `when`; Envoy Calder of the Lattice arrives on the plaza once the road is open. Her dialogue starts the Lattice quest, offers to "restore" Kaj-7 (a tension Kaj-7 raises afterwards), and takes an answer either way; the join choice requires the `act2` flag, so Act 1 can only warm or cool the relationship. The journal shows standing.
**Why**: "Lattice introduced" is an exit criterion and Act 2 needs the faction shape settled now. Reputation as numbers plus the casting rule as a mechanic means every future faction line changes companion feelings without hand-written approval blocks.

## D-075 — The demo slice: five stages, one boundary, no debug keys
**Date**: 2026-09-08
**Decision**: The Waking runs gate → relay → deeper → throat → choir: the gate road, Relay Station Nine (Pell, the hive, Halden's log), the Beacon raised to depth 2 and a depth-2 extraction, the Undercity Throat (the Warlord, then the first lucid Choir fragment). The fragment's dialogue sets `rules/demo.end_flag`; the end panel shows once with the run in numbers and the choice made, then the party is home with the story intact. Generic hooks added for it: trigger effects `victory_flag` (raised when the fight the trigger started is won) and `grant`; a once-trigger that starts a fight is spent only on victory, so a wipe lets the player return for the boss; extraction sets `extracted_depth_<n>` and upgrades set `building_<id>_l<n>` so quests can read them; Bastion buildings are interactable (the Beacon launches Shards, the Workshop opens the Bastion screen, the Arcanum the Weave, the Med-bay heals). The "deeper" to "throat" step still needs a hand-off the slice does not author yet (a trigger or dialogue after the depth-2 extraction); the test sets the stage. `docs/content-schemas.md` and `test_content_schemas.gd` pin the required fields of every kind.
**Why**: "Playable start to demo end without the debug keys" needed the Beacon to be a place and the boss to be retryable. The slice is authored thin (one map per beat) so the tooling is proven before the writing pass; length is nowhere near the planned 2–3 hours and is logged as such.

## D-076 — Front end: title, death-stakes at new game, settings with rebinding
**Date**: 2026-09-08
**Decision**: A cold start shows the title (New game → Story-Protected or Mortal, Continue from the autosave, Load via the slot list, Settings, Quit) inside the world scene; tests and CLI staging skip it (`Engine` meta set by the test runner, any `--` argument except `--title`). New game wipes the books and the story and stores the death-stakes choice as the narrative flag `mortal_mode`; `rules.story_protected` follows that flag on every load, so the mode rides with the save. The system menu is the pause menu and gains Settings and Title screen. Settings (fullscreen, master volume, pad glyph style) persist in `user://settings.json`; bindings can be rebound per action (press the new key or button; Esc keeps the old) and persist in `user://input.json` as overrides on top of `InputActions.BINDINGS`; a reset restores defaults. `Glyphs` names pad buttons for footers in Xbox or PlayStation terms, detected from the first connected pad unless the player picks one.
**Why**: A demo starts at a title screen, not in a yard; the death-stakes choice belongs at the start of a story, not in a rules file; and a controller-first game needs its bindings and glyphs to be the player's.

## D-077 — Audio is a content kind with synthesised placeholders
**Date**: 2026-09-08
**Decision**: `audio` entries (kind sfx or music) carry a `synth` spec that `SynthWave` turns into a stream at first use; adding `file` beside the sidecar replaces it without touching code. Abilities and walkable tiles carry their own `sound`; every other event (UI confirm/cancel/move/toast, combat hit/miss/death/turn/summon/stealth/vent, explore footstep/pickup/door/extract/level-up) is a name in `rules/audio` mapped to an entry. `AudioDirector` plays one-shots from a small pool, keeps a log for tests, and crossfades music between two players when the state changes: title, Bastion, explore per biome, combat. Footsteps follow the leader's cell.
**Why**: A silent demo reads as broken, and no audio assets exist. Synthesised placeholders make every event audible now and make the missing-asset list a grep for entries without `file`.

## D-078 — Steam behind Platform: one contract, two backends, Steam never required
**Date**: 2026-09-09
**Decision**: `PlatformBackend` is the full contract (availability, user, achievements, stats, cloud files); the null backend keeps achievements in memory and a cloud folder under `user://cloud_null/`, and `SteamPlatformBackend` talks to the GodotSteam singleton through dynamic calls so the scripts load without the extension. `PlatformService` picks Steam only when the singleton exists and `steamInitEx` succeeds with the app id from `steam/steam.json` (0 in the repo, so nothing initialises by accident). Achievements are content (`achievements` kind: name, Steam API name, conditions on the narrative); the world evaluates them at every autosave and after banking. Saves push to the cloud store as they are written and missing ones are pulled at startup; local files always win. `steam/steam_input_template.vdf` mirrors `docs/controller.md`. Both backends run the same contract test; the Steam half only asserts live behaviour when a client is up, which CI never has.
**Why**: GDD §4: all Steam calls behind an interface so the game runs Steam-free from the open repo. Achievements as conditions on flags means every future story beat is one JSON file away from an achievement.

## D-079 — Release: one tag, three builds, Linux smoked in CI, macOS ad-hoc, placeholders ship
**Date**: 2026-09-09
**Decision**: `v0.2.0-demo` is the first tag. The first tag run found the macOS universal export refusing to build without ETC2/ASTC textures enabled (`rendering/textures/vram_compression/import_etc2_astc`), which is now on in project.godot; the tag was moved to the fix. `release.yml` runs the tests, exports Windows, Linux and macOS from the same commit, boots the Linux export under Xvfb with `--screenshot --title` and fails the job if no frame is written (the `smoke-title` artifact keeps the frame), then packages and attaches the three archives to a GitHub Release. The exports carry `content/*.json`, the example mod and `steam/*.json` so the registry and the platform config load from the pack. macOS is ad-hoc signed and not notarized (no Apple developer account; Godot's Linux export cannot notarize anyway), which the release notes explain with the right-click → Open workaround; a real signing path is an M4 task once an account exists. The Steam demo depot layout is `steam/app_build.vdf` (three depots, ids 0, mapping `build/<platform>/`), unusable until Steamworks assigns ids. The art decision from D-075 lands here by default: no artist is attached, so the demo ships generated placeholders, and the title screen says "placeholder art and audio, real ones in progress" with the version. `docs/store-page.md` lists every store asset with its size and source; the only ones the repo can supply are the CI screenshots.
**Why**: "Never break the exports" had never been tested by a tag, and the smoke step turns a green export into evidence that the binary boots. Ad-hoc macOS and placeholder art are the honest options available today; both are logged in gaps as owed.

## D-080 — Saves are stamped with the content they index into; a mismatch is refused
**Date**: 2026-09-09
**Decision**: Save format v2 carries two fingerprints from the content registry: the whole content set (every entry of every kind, mods included, provenance stripped so a pack and a checkout hash alike) and the one entry the save's deltas index into: the handcrafted map, or the Shard template plus `ShardGenerator.LAYOUT_VERSION`. `SaveSystem.content_check` runs before the world is touched: a save whose location fingerprint is missing (v1), whose map or template no longer exists, or whose map or template has changed is refused with the reason, in the load toast and in every save list, which marks it "✗ content changed, needs a new game" and disables the row. A save written against other content whose location still matches (a balance number, a mod added) loads and is marked "content changed". The generator's layout version is bumped by hand when the same template, seed and depth would lay out differently.
**Why**: The exit criterion was "a save from a different content set is refused, not misloaded": dead-enemy and collected-pickup deltas are indexes into a map's placement lists, so a changed map silently moves them. Refusing only on the entry that matters keeps a balance patch from bricking every save, which a whole-set check would have done; the whole-set fingerprint is kept for the list and for support ("which content wrote this?").

## D-081 — Balance is a harness first: bands on naive play, and the Warlord learned to punch
**Date**: 2026-09-09
**Decision**: `test_balance.gd` plays every story fight of the demo (gate ambush, relay hive, the Warlord at level 2 and 3), the road as a player walks it (two fights, no Med-bay), and a Shard at each demo depth, twenty combat seeds each, with the Act 1 policy (hit the nearest thing, else walk at it), and prints a table: win rate, rounds, HP left, members downed. Each row holds a band; the naive policy is the floor, so the low edge means "winnable played badly" and the high edge "not a walkover". Tuned against it: the gate ambush is three scavs (was two, over before the enemy moved), the hive keeps two drones plus the mother, and the Warlord is 24 HP × 3 with evasion 15, a Chrome-Addict escort, and a new 2-AP finisher (`chrome_haymaker`, 5–9) instead of four 1-AP fists, which lands at a 75% naive win over two and a half rounds with half the party's HP gone. Found on the way: the controller archetype fell back to ranged kiting after its control effects, so the Warlord, whose only damage was a fist, spent every turn netting and walking away and lost every fight; a controller whose damage is melee now closes like a rusher. The `deeper` stage of the main quest hands off to `throat` in data: a stage with `next` advances when its objectives are done (checked before every autosave, with `next_toast`), so the test no longer sets the stage by hand.
**Why**: "Two external playtesters finish the slice" needs people the repo does not have; the harness is what can run on every PR and what a human pass will be measured against. The biggest finding is not tuned here on purpose: with 4 AP and 1-AP basic attacks, every body swings four times a turn for ~10 damage against 12–20 HP, so trash fights end in one or two rounds and initiative decides them (the naive party still wipes 30% of depth-1 Shards through chained fights with no healing). Pricing basic attacks at 2 AP with cheap 1-AP utility is the obvious experiment and touches every ability and a dozen tests; it is a feel decision for a human with the harness, logged in gaps, not a blind change.

## D-082 — M3 is systems, then content authored thin and wired; writing is a track, not a session
**Date**: 2026-09-09
**Decision**: `docs/m3-plan.md` schedules nineteen sessions in two phases. Phase A (S28–S37) builds what the campaign cannot be authored without: the combat-economy decision from D-081 taken first with the harness and a playtest; items, affixes and cyberware; races 6–10 with their hooks as generic rules; progression to level 12 with multiclassing and the subclass mechanics that are still strings; faction joining with vendors, areas and locks; the Ghost Markets and the Null Cathedral; the Archive, Garden and Quarters plus account unlocks; campaign tooling for branches, scripted sequences and an ending state machine; romance. Phase B (S38–S46) authors the game in story order: companions 4–6, Act 1 complete, Act 2 in two halves, Act 3, endings, the race-reactivity pass, modding docs with a validator, and a close that tags `v0.3.0` behind a scripted full-campaign test. Writing, art, audio, playtesting and the Steam and Apple accounts are parallel tracks that need people; sessions never block on them. "Content complete" is defined as every beat wired in data and walked by a test in both death-stakes modes; hours and prose are the writing track's and are logged as measured.
**Why**: The GDD's M3 line is thirty hours of campaign; no session authors that. S22 showed a session can wire a beat thin and prove it, and M2 showed art as a named track kept fourteen sessions unblocked. Taking the AP decision first avoids retuning three acts; systems before content avoids authoring twice; story order keeps every session's test building on the last.

## D-083 — Every attack costs at least 2 of 4 AP; the AI focuses fire and rushers break off once
**Date**: 2026-09-09
**Decision**: Every ability that deals damage costs one more AP than it did (basic attacks 1 → 2, heavy attacks 2 → 3); abilities with no damage (vent, cool down, shadow step, summons, shields) keep their price. With 4 AP a turn that reads as two attacks, or one heavy attack and a self action, movement free. The experiment ran first as runtime overrides in the balance harness, content untouched, and the table decided it: naive-policy rounds went from 1.6 to 2.0 on the gate ambush, 1.6 to 2.1 at the hive and 2.4 to 5.5 against the Warlord, members downed per win fell on every row, and both sides' damage halved together, so the tactical vocabulary (cover, flanks, marks, roots) is a larger share of each fight. Retuned on the new economy: the Warlord is 16 HP × 3 with evasion 10 (a 70% naive win over four rounds with half the party's HP gone, then 60–65% once the AI focused fire); Rusted Undercity gangs are one or two bodies at depth 1 (`group_size` [1, 2]), depth still adding groups and elites. AI: every archetype now targets the lowest-HP hostile it can reach this turn (walk plus longest range) instead of the nearest; a rusher at or under `ai_retreat_hp_fraction` (0.3) of its HP, with an ally still up and a hostile within two cells, breaks off once toward its allies, gaining at least two cells; a lone survivor never runs and a retreat happens once per fight, so nothing can stall. Harness after the session: gate 100% / 2.0 rounds, hive 95% / 2.8, Warlord 60–65% / 4.2, road chain 95%, Shards 60% / 55%.
**Why**: With 1-AP attacks every body swung four times a turn for ~10 damage against 12–20 HP; fights ended before positions mattered and initiative decided them (D-081). Two actions a turn is the genre's shape for a reason. The experiment-first method (overrides in the harness, then content) is how the next economy question should be run too. Focus fire and retreat were the two AI gaps every playtest note would have listed first; both are bounded so the soak stays green. No external playtest existed to triage, which is still the top gap.

## D-084 — Items are instances of content: bases, affixes by rarity, slots from rules and race
**Date**: 2026-09-09
**Decision**: Two kinds. An `items` entry is a base with a slot (weapon, armour, trinket, cyberware), stat mods, a damage bonus, a drop weight, a merchant price, an optional `min_rarity`, optional `families`, and an optional Workshop recipe. An `affixes` entry is a prefix or suffix with its own mods, the slots it may sit on (or `any`), a weight and a minimum rarity. What the player owns is an instance: `{uid, item, affixes, rarity}`, rolled by `ItemSystem.roll_drop` from the run's seeded RNG (a weighted base that fits the rarity, then as many affixes as `rules/loot.affixes_by_rarity` allows, no repeats, each allowed on the slot). Instances ride the run haul (`RunState.items`, lost on a wipe) and bank into `Ledger.items` on extraction; off-Shard drops bank at once. Equipment is `Ledger.builds[member].equipment` keyed by slot key, so a loadout saves with the books and `PartyBuilder.member_specs` adds its stats and damage bonus beside growth, subclass and talents. Slot keys come from `rules/loot.slots` (weapon, armour, two trinkets) plus a race's `cyberware_slots` (Chromed: 2, the GDD's extra equipment slots as data). Drops: enemies by tier (`drop_chance_by_tier`, `drop_rarity_by_tier`; an entry's own `loot.item_chance` / `item_rarity` win), loot crates as a pickup whose `grants.item` drops at the pickup's rarity (so vaults hold rare-plus gear through D-071), the Fence stocks items (`effect.item`), and the Workshop crafts recipes at its level for Salvage. The pack screen (I at home, or the system menu) lists slots, the pack and the recipes per member; Enter unequips, equips into an empty fitting slot or swaps, or crafts.
**Why**: GDD §12 and D-071 left rarity with nothing rare to give and Chromed with a hook and no equipment. Bases plus affixes is the smallest shape that makes a rare drop read as one, and keeping every number in content means the S28 method (override in the harness, then edit) covers gear too. Two bugs the tests caught on the way: reading the rules' slot array by reference and appending to it, and `build_for` dropping keys it did not know, so equipment must be carried by every writer of a build.

## D-085 — Race hooks are a trait vocabulary any entry can carry
**Date**: 2026-09-09
**Decision**: The five M1/M2 races carried their GDD hooks as strings nothing read. They are now `traits`, a block of generic keys with one reader each: `resist {damage_type: fraction}` (negative is a weakness; surfaces deal typed `corrosive` damage now), `regen_on_surface {surface: hp}`, `detect_hidden N` (hidden hostiles within N cells with line of sight are revealed when the detector's turn begins), `ability_damage_bonus {ability: n}`, `heal_immune_types [types]` (a vent-style heal of that damage type does nothing), `bonus_abilities [ids]` (added beside the class list), `salvage_bonus f` (summed across the standing party when a haul is banked), `mend_after_combat f` (a fraction of max HP back on victory), `talent_cost_mod n` (Aether price shift, floored at 0), and `tags [..]` (read by the new `race_tag` condition; the reactivity hook for S44). Traits flow race → origin merge (`PartyBuilder.merge_traits`: numbers add, arrays union) → PartyMember → Combatant, and any `enemies` entry can carry the same block. The ten races: Trueborn (cheaper talents), Chromed (cyberware slots, D-084), Aetherborn (an innate `weft_spark` whatever the class, weak to tech), Synth (immune to corrosion, arcane heals do nothing), Rootkin (biogrowth feeds them), Hollow (immune to corrosion, a quarter off arcane, tagged unnerving), Splicekin (senses hiders at two cells, +1 on strikes), Vaultkin (playable now: sees hiders at three, half corrosion, a fifth more Salvage), Skyborn (agile, tagged old_world), Swarmborn (mends a quarter after every fight, tagged disguise). Four overlay kinds join the paper-doll (echo, pelt, sky, swarm).
**Why**: GDD §15: a race is a registry entry, and "if adding one requires engine changes the architecture is wrong". Ten entries with a shared vocabulary is the test of that; the vocabulary's readers are the only code, and each is a rule an origin, a subclass or an enemy family can use next. Left as tags on purpose: Synth repair with parts, Skyborn knowledge checks, Swarmborn disguise and Hollow unnerving are dialogue and NPC reactions, which is the S44 pass over finished lines.

## D-086 — Progression v2: levels split between two classes, capstones at eight, subclass identities as effects
**Date**: 2026-09-09
**Decision**: The party level runs to 12 on a twelve-step curve, with talent tiers at 1, 4, 7 and 10 and two new talents per branch above tier 2; the Weave Tree was already cross-class (any member buys any branch) and stays so. Multiclassing is a build choice from `multiclass_level` (5): a member names a second class and then moves levels into it one at a time at the Weave, never more than the levels past the gate, so the main class always keeps five. `Progression.class_levels` splits the party level; the second class contributes its base abilities (not strike), its growth per level held there and its own `unlocks` at that count. A class `capstone` ability unlocks at `capstone_level` (8) levels in that class, so a pure build reaches it at 8 and a 12 split 8/4 still does; the Arcanum resets the split with the subclass and talents. Six capstones ship. The subclass identities that were plain attacks are now effects the vocabulary carries: `chain` (Storm Lance arcs half its damage to the nearest hostiles within two cells of the mark, never chaining from a chain), `taunt` (Shield Bash: the target's brain must attack the taunter while it lasts), `poison` with `spread` (Plague Hex ticks at the end of the victim's own turn and jumps to adjacent teammates with one turn less, so it burns out), `counter` (Riposte is a self stance: any adjacent attack, hit or miss, is answered), `aoe` (Null Field silences everyone within a cell of the target; friendly fire follows the rule), and `summon_count` on summons with party-side summons (Drone Swarm calls two drones, Deploy Turret one turret; they are the player's combatants, drop nothing, and are freed with the fight). One attack roll is one `_strike`, used by the primary, every area target and nothing else.
**Why**: GDD §8 in full: levels 1–12, multiclassing from 5, capstones at 8+, subclasses that are what their names say. Splitting levels rather than tracking XP per class keeps the single party level (D-064) and makes the choice reversible at the Arcanum. Every new effect is a key on an ability, so an enemy family can use chain, taunt or poison next session without code. Left open: the second class has no subclass and no resource loop of its own; the demo cap of 6 is gone because the demo content cannot reach past level 4 anyway.

## D-087 — Joining a faction is one narrative field, and everything it opens or closes is a condition on it
**Date**: 2026-09-09
**Decision**: `NarrativeState.faction` holds the joined faction id ("" until Act 2 commits) and saves with the story; `join_faction` sets it once, with the flags `joined_<id>` and `faction_locked` for content that prefers flags. Joining is an effect key (`join_faction`) on a dialogue choice or trigger; the world gates it on the act flag the faction names (`joinable_act` → `act2`), refuses a second oath, applies the faction's `join_reputation` deltas (its own +3, each rival −2) through the same path every reputation change takes, so every companion's approval moves by the casting rule (D-074) with no hand-written blocks, then autosaves. Two conditions read it: `faction` (the joined id, or "" for none yet) and `not_faction`. Each faction entry names its `envoy` (an `npcs` entry on the plaza once the road is open, three now), its `vendor` (a `merchants` entry) and its `area` (a small map off the plaza whose transition carries `when: {faction: id}`), and its offer quest has a `joined` stage. The other two envoys drop to a one-line `locked` node once you are sworn. The journal's standing line says whom you are sworn to.
**Why**: Act 2 is "The Choosing", and the choice needs to lock the others, open a place and a shop, and be felt by the party, in data. One field plus two conditions does all of it, and S36's faction-locked transitions and S40's faction paths are already expressible: `when: {faction: ...}` on anything. Rootched has no leaning companion until Yev (S38), so its casting rule can only wound for now; the test says so.

## D-088 — The Ghost Markets: a surface that jams tech, and a harness party that is dressed for the depth
**Date**: 2026-09-09
**Decision**: The third biome is content in the S17 shape: a `biomes` palette, a `shards` template with its own tiles (rolled shutters for walls, arcade tile, failing lights as the grate variant, market stalls as the cover block, echo static as the new surface), a merchant, and a family. The family spans all five archetypes and uses the trait vocabulary: Failed Hollows (rushers, immune to corrosion), Choir Cultists (ranged arcane), Echo-wraiths (stealthers, a quarter off physical), Cantors (summoners calling Echo-wisps, which stay in the family), and the Choir Voice as the controller boss (hushes casters, resists arcane). Echo static is the first surface that changes damage rather than hit chance: `rules/combat.surface_resist` maps a surface to `{damage_type: fraction}` and whoever stands in it shrugs that off, on the same clamp as trait resistance; static halves tech. The Beacon at depth 3 finds the Markets. The balance harness gained a row per biome at its unlock depth, and its party is now geared for its level (first subclass, every affordable talent, a common weapon and armour each, a Workshop level per depth), because a level-6 party with level-1 numbers told us nothing. Floors per row are low where the naive policy is hopeless: depth 3 extracts about 15% in either biome, logged in gaps for the M4 balance pass rather than tuned here.
**Why**: Two biomes proved biomes are data; the third is the first with the Choir in it, which Acts 2 and 3 need, and the first family added without touching the generator or the brain. A damage-side surface was the missing half of the surface vocabulary (evasion, amplification, chaining, corrosion, now jamming) and the Null Cathedral's silence surface can reuse it. Dressing the harness party was overdue: without it every row past depth 2 would have failed for reasons that are not the content's.

## D-089 — The Null Cathedral opens on a story flag, its fonts take the voice, and the Loom Approach remixes every family by rule
**Date**: 2026-09-09
**Decision**: The fourth biome is content in the S17 shape (palette, template, five tiles: nave walls, flagstone, the choir lattice over the crypt, pews as cover, null fonts), with the Sacristy Keeper as merchant and a family across all five archetypes on the trait vocabulary: Null Husks (rushers, half off arcane), Inner Voices (ranged arcane), Hollow Choirists (stealthers, Hollow-tagged), Warden Constructs (controllers whose lance silences, three quarters off arcane and weak to tech), Sacristans (summoners raising Husks) and the Organist as boss, whose Great Hush silences everyone within two cells. Null fonts are the third kind of surface: `rules/combat.surface_status` maps a surface to statuses laid on whoever starts a turn in it, and the null font silences for one turn, so casters must step out of the basin before they can answer. No Beacon level reaches the cathedral; a Shard template may carry `requires_flag`, checked beside `requires_unlock` wherever a launch is offered, and the cathedral waits on `null_cathedral_found` (Act 2 sets it) while the Loom Approach waits on `loom_located`. A template with a `remix` block is expanded by `ShardGenerator.expand_remix` when it is entered: its enemy pool becomes every non-boss, non-minion entry of the families it lists and its surface pool the union of the templates it lists, so the Loom Approach mixes all four families and all seven surface kinds without naming one of them, and the Organist holds its door from depth 1. CI renders both; the harness carries a row for each with low floors.
**Why**: The GDD's four families and the Act 3 dungeon that "remixes every system" needed to exist before Acts 2 and 3 are authored (S41, S42). A status-laying surface completes the surface vocabulary (evasion, amplification, chaining, corrosion, jamming, silence) and the remix rule means the final dungeons are one JSON block each, and stay current as families grow. A flag gate rather than a fourth Beacon level keeps the Bastion at three levels per building and ties the deepest Shards to the story that finds them.

## D-090 — Seven buildings: the Archive reads fragments that are story, the Garden heals and distils, the Quarters host scenes, and the account keeps what a playthrough earns
**Date**: 2026-09-09
**Decision**: Three `buildings` entries join the plaza in the S5 shape (three levels, costs, `effects`, blurbs). The Archive reads `lore` entries, a new kind (name, text, order, source): a `lore_fragment` pickup in every Shard pool grants `lore: true`, which finds the next unfound fragment in history order and stores its id in `NarrativeState.lore`, story rather than haul, so a wipe keeps it; the Archive at level 1 renders the found fragments in order with gaps for the missing (`ArchiveMenu`), and at level 2 pays an Aether of insight per new fragment. The Garden's `heal_fraction` adds to the Med-bay's on return (the effects merge sums by key), tending it heals its fraction with Rootkin doubled, and level 2 distils an Aether on every extraction (`aether_on_return`). The Quarters read `scenes` on companions (id, label, dialogue, `requires`, the Quarters level needed): a scene a recruited companion qualifies for and has not had is offered from the building, plays as ordinary dialogue and is marked `scene_<id>_seen`; Sera, Kaj-7 and Dax each have one at approval 3. `Account` (`user://account.json`, outside every save) holds keys a playthrough earns: every autosave grants `origin:<id>` for origins whose `unlock_flag` the narrative has set, and the creator hides such origins until the account has them; the Beacon Keeper origin opens after first contact with the Choir. Loadouts and cosmetics as account unlocks wait on content that uses them.
**Why**: GDD §12 names seven buildings and account-level unlocks; the Archive is the lore delivery the story spine needs (the eight fragments are the Sundering in order), the Quarters are the room S37's romances need, and the account is what makes a second playthrough start somewhere new. Fragments as story rather than haul follows D-032: nothing about the plot is ever lost to dice.

## D-091 — Campaign tooling v2: branches, sequences, map edits and the ending state machine are all effects and conditions
**Date**: 2026-09-09
**Decision**: Four additions, none of them new machinery. Quest stages may declare `branches` (`[{when, next, toast?}]`): once the stage's objectives are done the first branch whose conditions hold wins, `next` is the fallback, and a stage with branches and no objectives is a pure fork. Every effects block now goes through one `apply_effects` (triggers, dialogue choices, sequence steps) and three keys join it: `map_edits` (`[{map?, cell, tile}]`, recorded per map in `NarrativeState.map_edits`, applied now on the current map and on every later entry, the latest edit of a cell winning, Shards untouched), `sequence` (a list of steps, each an effects block plus an optional `camera` cell or "leader" and a `pause` in seconds; input is off and the party stands still while it runs, pauses are skipped under tests, and a step that opens a dialogue or starts a fight is the last to run, the world taking it from there), and `ending` (resolve and show). Endings are content: `endings` entries with `when` conditions, a `priority` and an `epilogue` per companion (alive, dead, absent); `Endings.resolve` picks the highest-priority ending that holds, so the Weaver's Mend (all three Keys, a spared fragment, every recruited companion at approval 5 through the new `party_approval_min` condition) outranks the three faction endings, and a priority-0 drift with no conditions is what nobody chose. The ending panel shows once, flags `ending_<id>`, lists the fates, and returns to the title.
**Why**: Acts 2 and 3 fork on the oath, play a catastrophe, change the world behind the player, and end four ways plus one; S40–S43 can now author each of those as JSON on maps and quests. Keeping them as effects and conditions rather than a scripting language means the S19 vocabulary still describes every map, and the validator in S45 has one grammar to check.

## D-092 — Romance is Quarters scenes, one string of state, and no jealousy
**Date**: 2026-09-09
**Decision**: A romance is a companion's `scenes` marked `romance: true`, gated like any scene by approval, the Quarters level and `requires`; the story keeps one string, `NarrativeState.romance`, the companion the leader is committed to. The `romance` effect (`{"commit": id}` or `{"end": true}`) and three conditions (`romance`, `not_romance`, `romance_open`) are the whole vocabulary. Exclusivity is engine-side and jealousy-free: while `romance` names someone, other companions' romance scenes are not offered and nothing else about them changes; a commitment to a second companion is refused until the first ends. Scenes may be `once: false` (they stay listed until their own `requires` close them, so "not tonight" is not "never") and `dead: true` (offered for a companion with `<id>_dead` instead of one in the party). A partner's death in Mortal mode calls `lose_romance`: the string clears, `romance_<id>_lost` is set, `romance_<id>` stays as history, and the death scene reads the flag; Story-Protected mode never gets there. Endings may write `romanced` and `lost` epilogue lines beside alive, dead and absent. Shipped for Sera, Kaj-7, Whisper and Yev: a spark (approval 5, Quarters 1, after the trust scene), a commitment (approval 7, Quarters 2), a night with the option to end it, and the empty bunk; refusal at either door costs no approval. Whisper and Yev exist as companion entries with placeholder recruitment, banter and quest shapes that S38 writes; Whisper leans no faction, which companions may now do.
**Why**: GDD §3 and §7 name four romances written mature and §12 puts them in the Quarters; S35 already gated scenes on approval, so romance is the same mechanism plus a single piece of state for exclusivity. One string rather than per-companion flags makes a mod's new romance exclusive with the shipped four for free. Jealousy-free because the casting rule already wounds companions on the faction choice: the romances are where the party is kind.

## D-093 — Companions 4–6 stand on story beats; six recruits and a party of four means a bench
**Date**: 2026-09-09
**Decision**: Whisper, Cinder and Yev are companion entries like the first three: a recruit graph that doubles as talk, banter, a personal quest with a Shard site and every path (with them, alone, posthumous), a trust scene, and the casting rule on the recruiting choice (one of the party approves, one is wounded). They stand on handcrafted maps behind story flags, which `npcs` companion placements now honour through `when` (Whisper in the relay rafters once `hive_cleared`, Cinder at the source once `warlord_beaten`, Yev on the plaza once `choir_contact`), and a dead companion never stands in again. Their quests intersect the plot as data: Whisper's recall beacon is Lattice hardware signed by an archivist (a thread for Act 2), Cinder's tether runs to a Key under the Null Cathedral and following it sets `null_cathedral_found` (with or without him), Yev's cuttings are a Rootched frame growing a Key out of her grove and the three answers move Rootched standing. A companion may lean no faction (Whisper). Six recruits and a party of four (GDD §7) needs a bench: `NarrativeState.benched` holds recruits waiting at the Bastion, `add_companion` benches the newcomer when the party is full, `spawn_party` and banter use the walkers, and the Roster item on the system menu (off-Shard only) swaps them with a respawn on the plaza. The `recruited` condition now means walking with the party (recruited and not benched), so a companion at the Bastion does not speak from a Shard; Quarters scenes, `party_approval_min` and ending fates still read the whole recruited list. One quest site opens per pickup check: anything else underfoot waits for the next frame.
**Why**: §3 names six companions and §7 a party of four, so the sixth recruit had nowhere to go without a bench; the smallest one is a list on the story and a menu. Placing the three behind flags rather than new maps keeps S38 to content plus two small engine rules (`when` on companion placements, one site per check), which S39 and S40 needed anyway for Act 1 complete and the loyalty quests.

## D-094 — Act 1 complete: the demo slice grown to the act, the demo boundary a switch, dialogue choices with the whole effects vocabulary
**Date**: 2026-09-21
**Decision**: The Waking now runs gate → relay → deeper → expeditions → throat → choir. The relay station is Act 1's hub: Pell stays (or, sent away, waits on the plaza) and after the log offers the three lost crews as a main quest of three Shard sites in sequence (Ostrand's marker, Vane's rig that can start a fight around the party, Marrow's cistern with a choice), whose flags open the throat. The throat descent is three maps: the Warlord's throat, the Flooded Gallery with a Choir cantor, and the Source, where a scripted approach hands over to first contact, and the fragment now says outright that the Sundering was chosen and refused; the line lands in the Archive as a fragment, and Cinder, if walking, confesses his yes. First contact sets `act1_complete` and `act2`, which is what the envoys' oaths wait on. Whisper and Cinder are recruited on the way (both bench, the party being full). Three small engine rules: dialogue choices now carry the whole effects vocabulary (the runner applies the story part, the world applies the rest, once per choice), `enemies` entries may be placed by `offset` from the leader so a Shard-site dialogue can spawn a fight, a `lore` effect names a fragment, and NPC placements refresh after a fight or a talk so a companion waiting on a flag appears at once. The demo boundary is `rules/demo.enabled`, off in the base content; the demo build launches with `--demo`, which turns it on. Length as measured by the walkthrough: nine beats, four handcrafted fights, four Shard runs; roughly two to three hours of play, not eight; the rest is the writing track's.
**Why**: Act 2 needs a whole Act 1 behind it (the plan's S39 row), and the demo's end had to stop being the game's end without a second build of the content. Everything new is data on the S19–S36 vocabulary; the three engine rules were the gaps the content ran into (a site that fights, a placement that waits on a fight, a reveal that reads later) and each is one paragraph. The hours are logged as measured per the plan's running assumptions.

## D-095 — Act 2 part 1: faction paths as quests from the envoy, loyalty as a talk at approval 4, the Choir in borrowed voices
**Date**: 2026-09-21
**Decision**: Each faction's path is a main quest the envoy offers once you are sworn: a first task at a Shard site (the Lattice's leak to seal, the Rootched's seed to set, the Ashfound's shrine to burn), a report to a new NPC in the faction's area (Archivist Tal, Speaker Oriel, Forge-master Hesk), and a located Key as the reward (`key_<faction>_located`: the Lattice's under the Ghost Markets, the Rootched's in the Datacore's heart, the Ashfound's under the Null Cathedral's fringe) for S41 to build the delves on. The casting rule holds on the path's choice, not only on the oath: the site's faction-way choice carries approval for the leaner and against the rivals, and each site has a companion's call and a lie. The report NPCs take a walking companion's word (Whisper meets her maker, Yev asks the Speaker whose Key her grove grows, Sera asks Hesk who marked the Ninth Stair). Loyalty is a talk: every companion's recruit graph gains a `loyalty` start entry at approval 4 in Act 2 that offers once and can be declined and re-asked; the quest is one Shard site with a loyal choice, a broken one, and a companion's own call, resolved by a quest branch on `<id>_loyal`, with alone and posthumous paths. The Choir courts through a main quest that starts itself (`start_when` on quests, with a `start_toast`) once `act2` holds: three Shard sites in sequence whose dialogue nodes carry a `voice`, the walking companion whose banter the Choir repeats back (the panel reads "The Choir, in Sera's voice"), or the leader's own words from the source when nobody walks; refusing or listening is a flag per voice for the catastrophe to read.
**Why**: The plan's S40 row: the faction choice is the hinge, and the hinge needs a first consequence on every path, loyalty needs a beat per companion, and the Choir needs to talk. Paths as quests from the envoy keep the oath's locking from S32 and give S41 a located Key per faction without a fourth system. Loyalty at approval 4 sits between the trust scene (3) and the romance spark (5) so the Quarters, loyalty and romance are three rungs of one ladder. `start_when` and `voice` are two lines each and both are things Acts 2 and 3 will use again.

## D-096 — Act 2 part 2: Keys as biome-bound delves with keepers, the Loom from three resolved Keys, the catastrophe as one conditional sequence
**Date**: 2026-09-21
**Decision**: The three Keys are three main quests that start themselves when their Key is located, which happens two ways so every path reaches all three: the faction's own path locates its Key (S40), and the Choir's third borrowed voice names all three and finds the Cathedral, whatever you answer. Each delve is one Shard site bound to its biome (`shard_site.template`: the Ledgers under the Ghost Markets, the Heart in the Datacore, the Quiet under the Null Cathedral) whose dialogue starts a keeper fight; the victory sets the Key intact and resolved (`victory_flag` takes a list), a wipe leaves the site for the next run, and breaking the Key resolves it without a fight and forecloses the Mend. Three resolved Keys start The Loom, whose first stage is the catastrophe: a once trigger on the plaza spawn that runs one scripted sequence whose steps carry `when`, so prior choices pick the outcome. Listening to the third voice is the invitation; with it, the Choir takes the partner unless they are loyal (then they resist, on the plaza, in their own way), else Kaj-7 unless loyal, else Cinder unless loyal, else it called and nobody went; without it, the plaza is struck (map edits, the Garden path caved in) and nobody is taken. Taken means a `dismiss` effect: off the roster, out of the party, flagged `<id>_taken`, neither dead nor recruited, which the ending state machine reads as absent until S43 writes the lines. The sequence ends with `loom_located`, which opens the Loom Approach Shard; The Loom's second stage is where Act 3 starts.
**Why**: The plan's S41 row asks for delves per biome, the Loom's location, and a catastrophe shaped by prior choices; binding a site to a template and letting sequence steps carry conditions are the two smallest rules that make both data. One sequence with conditional steps keeps the catastrophe readable as a list of outcomes rather than a tree of triggers, and the same three inputs (the invitation, the partner, loyalty) are the three ladders S37, S40 and the courting built, so the catastrophe is where they pay off.

## D-097 — Act 3: the Loom's door is a Shard site that banks the haul, the Loom is one handcrafted hall, the confrontation is fights then a choice
**Date**: 2026-09-21
**Decision**: Act 3 runs on The Loom quest from its `located` stage: the Loom Approach (the S34 remix template, open on `loom_located`) carries one site bound to it, the Loom's door, whose dialogue is a `transition` into a handcrafted map; leaving a Shard through a door banks the haul at the threshold (`travel` does what extraction does, without the pad), so the run is not lost and there is no way back but the ending. The Loom is one hall that mixes every surface (mana, conduit, biogrowth, echo-static, null fonts, pews) with three rival strike teams as once triggers gated `not_faction`, so a sworn player fights the two rivals and the unsworn fight all three, then the Choir's Voice with its inner voices at the last door, then the heart: a scripted approach and a dialogue with the lucid fragment. The confrontation is two choices: spare the voice or end it (`choir_spared` is the Mend's condition), then set the Loom (Order, Completion or Ash by faction, the Mend when all three Keys are whole and the voice spared, or walking away for anyone), and every setting fires the `ending` effect, which the S36 state machine resolves. The Loom choice writes `loom_set_*` flags the endings do not read yet; S43 decides whether an oath-breaker who walks away gets the Drift.
**Why**: The plan's S42 row: the race, the remix dungeon, the Loom as a map, the confrontation as a fight and a choice; everything is on the existing vocabulary plus one rule (a door out of a Shard banks the haul), which the Loom Approach needed so that the final delve is not a haul-or-story trade. Rivals as `not_faction` triggers make the unsworn path the hardest without any new content per path. The ending fires here rather than in S43 because the machinery has existed since S36 and a playable Act 3 is a game with an end.

## D-098 — Endings read the hand on the Loom; fates climb a ladder; modifiers and credits are data
**Date**: 2026-09-21
**Decision**: The faction endings require both the oath and the matching setting at the Loom (`loom_set_order`, `loom_set_fusion`, `loom_set_ash`), the Mend requires `loom_mended` beside its three whole Keys, the spared voice and every companion at approval 5, and the Drift is what remains: a sworn player who walks away drifts, and the Drift's modifiers say which oath they walked away from. Companion fates climb a ladder per companion: dead (a dead partner reads `lost`), taken by the Choir (`taken`), alive as a partner (`romanced`), loyal (`loyal`), or alive; an ending that does not write the rung falls to the next, so a taken companion with no line is absent and skipped, and content is free to write as many rungs as it has words for. Every ending writes alive, dead and taken for all six companions and loyal lines besides; romanced and lost belong to the Mend. Endings carry `modifiers`, paragraphs with conditions that the panel adds after the summary: each broken Key, the ended or spared voice, the struck plaza, someone taken, and for the Drift and the Mend the oath. Credits are `rules/credits.lines`, rolled after the run, and honest about the parallel tracks. Each ending is an achievement on its `ending_<id>` flag.
**Why**: The plan's S43 row: every ending reachable, the secret one only on its conditions, the epilogue listing every companion's fate, with companion and key modifiers and credits. Reading the Loom choice makes the S42 confrontation the ending's cause rather than its narration. A fate ladder in the engine and rungs in the data means S44's reactivity pass or a mod can add a rung without touching code, and the panel never shows an empty line.

## D-099 — Reactivity is three condition keys and an act tag, and the test counts the budget
**Date**: 2026-09-21
**Decision**: Three condition keys join the vocabulary: `party_race` and `party_race_tag` read every walking member (the leader's `race` and `race_tags` stay the leader's), so an NPC can react to a Hollow behind you; `attribute` reads the leader's creator attributes with min and max, which is the skill check; `disguised` is true when the leader's race carries the `disguise` tag and their arcane control clears `rules/attributes.disguise_arcane_min`, so a Swarmborn passes some doors and is seen at others. Every non-banter dialogue carries an `act`, and `test_content_narrative.gd` counts gated lines per race per act and requires at least one for every race in every act, every origin tag used more than once, NPC reactions to the party, three disguise lines, and a skill check in every act. The pass itself is about forty lines: a race line for each of the ten in each act (Hollow and Swarmborn last and most, with Pell, Archivist Tal and Forge-master Hesk reacting to them), the keeper, choir-touched and corporate origins in two acts each, and Tech, Body and Arcane checks that recruit, carry and feel the Loom.
**Why**: GDD §6's reactivity budget and §11's skill, origin and race gates; the M3 exit criterion is ten races with reactivity, and a criterion the test counts is one that stays met as the writing track adds lines. The party keys exist because the interesting reactions are to who you brought, not who you are; the disguise is an attribute check rather than a die roll because the creator is the only place the player spends points and it should buy something in dialogue.

## D-100 — One validator for base and mods: the schema and cross-reference checks are a class, the tests and the CLI both run it
**Date**: 2026-09-21
**Decision**: `ContentValidator` (src/core) holds the per-kind required keys, the condition and effect vocabularies, and every cross-reference and well-formedness check the content tests had grown (companions, dialogue graphs, quests, maps, Shard templates, pickups, buildings, factions, npcs, merchants, items, endings, achievements, biomes, tiles, audio, origins, lore), and returns problems as strings naming the kind, id and source. `tools/validate_mods.gd` runs it from the command line on a folder of mods or one mod, prints the report, and exits 0, 1 or 2; CI runs it on `examples/mods`. The content tests keep their specific assertions and add one that the base game validates clean, so the base can never ship a problem the tool would report to a modder. The stranger's mod under `examples/mods/stranger` (a race, a class with a subclass and a resource, an ability, an item, a companion with a recruit graph, banter and a quest, and a map) validates clean and loads into a world in a test; it lives outside the scanned roots so it never changes the base game. `docs/modding.md` covers layout, the validator, and how to add every kind by copying a named base entry; the Workshop layout is noted for M4 as the same folder uploaded as-is.
**Why**: The plan's S45 row and the M3 exit criterion "modding docs": a stranger's mod that validates from the CLI and loads. Making the validator the same code the tests run means the vocabulary cannot drift between what the game accepts and what the tool reports, and the report reads as what to fix rather than which test failed.

## D-101 — M3 closes on a scripted campaign, a triage and a tag; M4 starts with a playtest
**Date**: 2026-09-21
**Decision**: `tests/unit/test_campaign_full.gd` plays three acts on every faction path and the unsworn, in Story-Protected and in Mortal mode where a companion dies mid Act 2 and the story carries on without him, to eight endings, through the same calls the keys use; fights fall by fiat because the harness measures them and this test proves wiring; it runs in its own CI job (`campaign`) under a budget, and the unit job skips it (`--skip=`). `docs/gaps.md` is retriaged at M3's end into what needs a person, balance for after the playtest, content that is wired but not dense, and systems for M4. `project.godot` is 0.3.0 and `v0.3.0` is tagged from main through `release.yml` once the close merges. `docs/m4-plan.md` schedules fourteen sessions that start with the first external playtest and end at `v1.0.0`, and adds no beats: M4 is balance, the people tracks, surfaces and shipping.
**Why**: The M3 exit criteria (m3-plan) are each a test or a doc, and the campaign test is the one that makes "content complete" provable rather than claimed. Fights by fiat keep the test about the story graph; the balance rows already exist for the fights and the playtest is what should move them. A separate CI job keeps the pull-request loop as fast as it was.

## D-102 — The playtest is a kit: a log of what the harness measures, a report against its bands, a script and a triage table
**Date**: 2026-09-21
**Decision**: A session cannot supply the two people M2 and M4 ask for, so S47 delivers what makes their afternoon count. `-- --playtest` attaches a `PlaytestLog` that appends one JSON line per event under `user://playtest/`: the session, every map entered with the mode and elapsed time, every fight with its result, rounds, HP left, downed, party level and enemy kinds, every run with its outcome, template, depth and haul, every dialogue choice by dialogue, node and text, and the ending. `PlaytestReport` turns a folder of those logs into the triage table (fights by map and enemy set beside the harness band for that map, runs by template and depth, minutes to each story map per session, choices by line, endings) and `tools/playtest_report.gd` prints it. `docs/playtest.md` is the kit: what to give a tester, the launch flag per platform, a two-to-three-hour script, ten questions in order, how to read the report, and the triage table with owners, filled in as pending until two people have played. The harness bands move only when the table has rows.
**Why**: The M4 plan starts with the playtest and orders S48–S50 by what it finds; without the log, what it finds is anecdote. Logging exactly the harness's measures means a person and the naive policy are compared on the same rows, and the report is the same table the balance sessions read. No personal data goes in the log, and nothing is written without the flag.

## D-103 — Balance pass 1, fights: medkits, a breather, a lighter depth curve, a softer boss with a wind-up, capped depth groups, lighter Cathedral and Approach, and a Choir that acts second
**Date**: 2026-09-21
**Decision**: Run as harness overrides first (D-083), then landed as data. Rules: `depth_hp_per_level` 0.15 → 0.10, `boss_hp_mult` 3.0 → 2.5, a new `boss_telegraph_round` 1 (a boss uses no ability of 3 AP or more in its first round, and the HUD says it winds up) and a new `rest_after_victory` 0.2 (the standing party regains a fifth of max HP after a win: a breather, not a Med-bay). Pickups: a `medkit` (`grants.heal` 0.4, a fraction of max HP for the standing party, never a haul) in every Shard pool at weight 2. Templates: `enemies.extra_groups_max` 1 on all five (depth adds at most one group), the Loom Approach from 6–9 groups of 2–3 to 4–6 of 1–3, the Null Cathedral from 5–8 groups of 1–3 to 4–7 of 1–2. The Choir: hymn bolt 2–5 at 85% → 2–4 at 75%, cultists' initiative 2 → 0, a cantor calls one wisp, not two. Measured on the lab (16 seeds, the naive policy, geared parties at the depth's level): the Undercity at depth 3 from ~15% to ~50%, the Datacore at depth 2 from ~30% to ~44%, the Cathedral at depth 3 from 0% to ~44% once its population is lighter, the Loom Approach from ~5% to ~31%, the Ghost Markets from ~25% to ~38%. The harness floors move to what was measured: Undercity d3 0.3, Markets d3 0.2, Cathedral d3 0.2, Approach d1 0.15, Datacore d2 0.25; the harness itself then read Undercity d3 45%, Markets 30%, Cathedral 40%, Approach 30%, Datacore d2 40%, the Warlord 80% at levels 2 and 3, the gallery 80%, and the Loom's Voice 35%, every row in band. The Markets stay under the plan's 40%: their lost fights are the long ones, a cantor with three or more Choir bodies kiting on echo-static while a naive party chases; cultist HP, cantor HP and the echo resist did not move them, and a human focuses the cultists, so that row is left at what naive play does and logged.
**Why**: The M4 plan's S48 row, run without the playtest it was meant to follow (the user's call). The naive policy is the floor a person will beat, so the levers chosen are the ones that help naive play without flattening the biomes' identities: heals and a breather address attrition, the boss levers address the pad fight, the population cuts address two templates that were simply overstuffed, and the Choir change makes its ranged line beatable by closing rather than removing the kiting. Every lever is a rule or an entry a mod can override, and the lab that chose them was the harness with overrides, so the playtest can re-run the same table.

## D-104 — Balance pass 2, economy and approval: measured, mostly left alone; the Beacon's first step pays from the road; the campaign asserts approval instead of setting it
**Date**: 2026-09-21
**Decision**: Two labs measured before anything moved. The Act 1 walk with real fights banks 14 salvage, 3 Aether, 39 XP (level 2) by the relay, 53/9 and 82 XP (level 4) by the Warlord, 60/14 and two Ciphers by first contact. A Shard extraction under the naive policy banks about 20 salvage, 4 Aether and 52 XP at depth 1, 27/4/54 at depth 2, 41/10/97 at depth 3, 25/12/76 in the Datacore and 21/16/108 in the Markets, so the Beacon's second step (40/6/1) is two runs, level 6 arrives around depth 3 and level 11 around the Loom, which is what the harness rows assume. Approval on the Lattice walk with the kinder choices: Sera 6 and Kaj-7 4 by first contact, Sera 8 and Kaj-7 9 by the Keys, with the courting worth three to whoever walks in front; on the Rootched and Ashfound walks the oath and the path move Sera by minus three or plus three and she still clears loyalty (4) before her roll of names and romance (5) by the Keys. Dax and the benched three stay low by design: the casting rule wounds him on two paths, and a companion at the Bastion has no sources until they walk. Changes: the Beacon's first step 15 salvage → 12, so the road pays for it exactly and Act 1's "raise the Beacon" objective does not send a player back for one salvage; `test_campaign_full.gd` no longer sets Sera's approval and instead asserts 4 by the loyalty and 5 by the Keys on every path; the playtest report's RUNS table adds XP and kills per extraction. Costs, loot ranges and the XP curve are otherwise unchanged; the harness assumptions and the measurements agree.
**Why**: The M4 plan's S49 row, and its done-when: a walkthrough that reaches approval 5 by the Keys without setting it. The measurement said the curves were already there for the companions who walk, so the honest change is the assertion that keeps it so, plus one cost that the road was a coin short of. Everything else waits for a person, as S47's kit intends.

## D-105 — Difficulty as a rules overlay entry; Iron Weave as an account flag over the autosave
**Date**: 2026-09-21
**Decision**: A new content kind, `difficulties`: `name`, `summary`, `order`, `default` (exactly one in the base game) and `rules`, an overlay `{rules entry id: {field: value}}`. The base game ships Story (a gentle depth curve 0.05, elites 1.25×, bosses 2.0× with +1 damage and two wind-up rounds, a 35% breather, softer ambushes), Balanced (the default: no overlay, the rules exactly as `rules/combat` says) and Tactician (depth 0.15 with +1 enemy damage every depth, elites 1.75× and +2, bosses 3.0× and +3 with no wind-up, a 10% breather, enemies that break off later), the names from GDD §11. The world reads every rules entry through `rules_entry(id)`, which writes the story's overlay over the registry's entry: the combat rules are rebuilt from it whenever the death-stakes are applied (new game, load), and the progression and loot accessors go through it too, so a difficulty may later touch XP or drops without new code. The difficulty is a `NarrativeState` field saved with the story; a save from before, or one naming a difficulty that no longer loads, plays on the default. The title asks the difficulty first, then the death-stakes; the validator refuses an overlay that names a rules entry or a field the rules do not have, and a base game without exactly one default. Iron Weave is a third death-stakes row: Mortal, with the autosave as the only save (the slots refuse to save and to load, the autosave item and F9 are closed, the Load row on the title is closed while a run stands, Continue is the one way back in), a wipe or a dead leader ends the run and deletes the save, and any other new game gives the run up. The account, not the save, carries `iron_active`, `iron_difficulty` and `iron_runs`, so a copied or swapped save cannot restart a run; an ending clears the run and keeps `iron_weave:<ending>` as an account key, with an `iron_weave` achievement on the flag. The campaign test now plays Story-Protected on Story, Mortal on Balanced, and an Iron Weave run on Tactician down the Lattice path with no slot save and no reload, nine endings in about 21 s. The combat checkpoint before every fight stands: an Iron Weave fight walked out of restarts from its checkpoint (no mid-combat save exists), which is logged in gaps rather than punished as a forfeit that a crash would also earn.
**Why**: The M4 plan's S50 row: three difficulties as rules overrides and Iron Weave as an account flag, done when each difficulty is a rules entry and Iron Weave completes the campaign test. An overlay entry is the smallest shape that lets a mod add a fourth difficulty by adding a file and lets the validator catch a mistyped field, which a hand-merged rules file would not; the account flag is the only place a permadeath promise can live, because the save is the thing the promise is about.

## D-106 — The creator is the first screen of a new game; appearance as two palette picks; the portrait is the rig
**Date**: 2026-09-21
**Decision**: New game runs title → difficulty → death-stakes → creator → the plaza. The creator opens over a closed title with a fresh sheet and the account's unlocks, nothing begun: Esc returns to the death-stakes page and no save, ledger or narrative has moved; Enter validates the sheet and only then calls `new_game` with it, so the sheet leads from the first step and the first autosave carries it. C at home still edits the sheet as before. Appearance is two rows, tone and accent, whose options live in a new `rules/appearance` entry (`tones`, `accents`: {id, name, color}); the sheet saves them as ids under `appearance`, empty meaning the race's default look, and validation refuses an id the rules do not have while the creator silently drops one that no longer loads. The party builder resolves the ids to colours for the protagonist only; the placeholder rig paints the tone on the head and the accent as a hair cap, never outside the silhouette, and a sprite sheet takes them through `skin` and `accent` palette roles when its sidecar names them (the placeholder sheets do not, so a Trueborn on the real sheet keeps the class tint until an artist adds the roles). The portrait is a 64×64 head-and-shoulders on the same rig, the race overlay stamped on the shoulders, shown at 3× on the creator; it is a placeholder in the same sense as the sprites, replaced by drawn portraits when they exist (S56), and S52 will put it in the dialogue panel. Origins locked on the account are listed under the Origin row with their `unlock_blurb` rather than hidden, and every attribute row says what its next point buys ("next point: HP 17 → 19", "at the cap", "no points left") from the same StatBlock derivation the preview uses. The creator answers the project's `confirm`/`cancel` actions as well as `ui_accept`/`ui_cancel`, so the pad and the rebound keys work on it as they do on every other menu.
**Why**: The M4 plan's S51 row and its done-when: the creator is the first screen of a new game. Opening it before `new_game` commits anything is what makes Esc honest. Two palette picks are the appearance the placeholder rig can actually show; a wardrobe of overlays would be guesses about art that does not exist yet, whereas a tone and an accent survive any rig. The portrait comes from the same generator so a mod race with an overlay gets one for free.

## D-107 — Every text panel has a face and a history: portraits from the rig, a saved log, a tracked quest on the HUD, the Roster and the Quarters as one screen
**Date**: 2026-09-22
**Decision**: Faces: the world builds a portrait per speaker from the S51 rig (`portrait_for`): the leader from the protagonist's class colour, race overlay and appearance; a companion from its race and class; an NPC from its `art.color` (and a `race` if an entry names one); nothing for the narrator, the Choir or any speaker without a body, in which case the panel wears the voice's face (`face_for`), so the Choir in Sera's voice looks like Sera. The dialogue panel gets a portrait column and takes the face and the history through callables, staying a renderer. History: `NarrativeState.log` ([{who, text}], capped at 120, saved) takes every line as it is shown, every choice as "→ text" in the leader's name, and every quest start or stage change as "Journal: quest — toast or stage summary"; the panel shows the last three lines of the open talk above the current one, the journal shows the last twelve of everything. Tracking: `NarrativeState.tracked_quest` (saved) or, failing it, the first active main quest, then the first active one, gives the HUD a fourth line ("★ The Waking — the stage · the next undone objective"); ←→ in the journal moves the mark, which the journal shows as "▶ … (tracked)". Roster and Quarters: one `RosterMenu` screen from the system menu's "Roster & Quarters" item and the Bastion's Quarters item (the latter still asking for the building): one row per companion the story has given you, walking, waiting or fallen, with approval, loyalty and romance, a face for the row the cursor is on, and that companion's Quarters scenes under the row while the Quarters stand (a note when they do not); Enter or ←→ on a companion swaps walking and waiting, Enter on a scene plays it, the fallen keep their scenes (the empty bunk) and nothing else. The old `bench_`/`take_`/`scene_` system items still work for scripts and tests.
**Why**: The M4 plan's S52 row and its done-when. Portraits from the rig cost nothing per speaker and follow a mod's races for free; drawn portraits will replace them file-for-file when the art track delivers, through the same `portrait_for`. The log lives in the narrative because it is the story's memory, not the UI's, and because a save without it would lose the panel's history on load. The Roster and the Quarters were two lists about the same people; one screen with a face is what the plan asked for and what a pad can drive.

## D-108 — Pad and Deck: glide by tilt, rumble as named pulses behind a setting, text size from designed sizes, a measured performance budget, and one door cache
**Date**: 2026-09-22
**Decision**: Analog: the walk speed follows the stick's tilt (`limit_length` instead of `normalized`, so keys still walk at full speed) and the combat cursor, after its first step and the long pause, glides at `CURSOR_REPEAT / clamp(tilt, 0.35, 1)`: a nudge creeps, a full tilt races, a crawl is never possible. Rumble: a `Rumble` class with named pulses (hit, heavy at six damage or more, downed, boss wind-up, victory, wipe, extract) on every connected joypad, recorded headless so tests can see them, behind `Settings.rumble`; the combat controller fires them from the same events the sounds use, and only for party targets. Text size: `Settings.text_scale` in four steps (normal, large, larger, Deck 1.5×) applied by `UiScale.apply`, which walks every CanvasLayer under the world, remembers each Control's designed font size in metadata the first time and sets designed × scale, so the setting moves up and down without drift and world-space labels (actors, buildings) are untouched; a fresh install on a 1280×800 Linux screen, or `--deck`, starts at Deck size and fullscreen, and saved settings win. The 1920×1080 canvas stays: the Deck shows it at two thirds with `canvas_items` stretch, which is what the text size compensates. Performance: `rules/performance` holds a budget (cells 3200, nodes 400, enemies 32, pickups 24, logic 2 ms, frame 16.6 ms); `tools/perf_budget.gd` (also `PerfBudget.measure` for the unit test) enters every Shard template at its largest size at depth 3 over six seeds and every handcrafted map, counts and times the game's own `_process`, fails on a count over budget and, with `--strict`, on time; `--perf` in a build puts fps and frame time on the status line for a Deck in hand. The lab that set the budget found one real cost: `adjacent_door` and `check_secrets` scanned every cell for doors every frame (about 6 ms of the 6–8 ms a Shard frame cost headless); `MapData.door_cells` now caches per kind until a tile changes, and a Shard frame costs about 0.3 ms.
**Why**: The M4 plan's S53 row. Its done-when, 60 fps on a Deck-class target, cannot be read here (no Deck, no rendering headless), so the session delivers what can be held: the budget, the tool, the counter, the one measured hot spot fixed, and the pad feel the plan names. Named pulses and a tree-walk scale were chosen over per-menu code because every menu already sets its sizes and every combat event already carries its target; adding two rows to the settings screen and two flags to the loop is the whole surface.

## D-109 — A saves screen with titles, thumbnails and a question; older autosaves kept; Music and SFX buses; a colour-blind pass; a settings screen that explains itself
**Date**: 2026-09-22
**Decision**: Saves: every save carries a `title` ("Weaver · The Bastion · Act 2 · level 6": the leader, the map, the act by the story's flags, the party level) and, where a frame exists, a 320×180 PNG beside the file, written from the viewport at save time and skipped headless; `SaveSystem.remove` and `move` carry the picture with the file. A `SavesMenu` screen with a save page (the three slots) and a load page (the slots, the autosave and its older copies), the row under the cursor's picture beside the list, opened from the pause menu's "Save game…" and "Load game…" and from the title's Load, which it returns to on Esc. Saving into a slot that holds something asks first and Esc keeps it; F5 and F9 stay the quick path to slot 1 without asking, as quick keys should. Autosaves rotate: the newest is `autosave`, the one before `autosave_1`, then `autosave_2`, `AUTOSAVE_KEEP` three, Continue taking the newest; Iron Weave keeps one, as it promised. The old `save_N`/`load_N`/`load_autosave` items still answer for scripts and tests but are no longer listed. Audio: `Music` and `SFX` buses under Master, made by the director if the project has none, the music players on the one and the pool on the other, each with its own volume in the settings and the master over both. Colour: a `ColorFilter` layer on top of everything, a full-screen shader that simulates protanopia, deuteranopia or tritanopia (Machado 2009) and pushes the lost signal into the channels that remain; one node, no colour in the game touched, hidden when the palette is normal. The settings screen groups its rows (Display: fullscreen, text size, palette; Audio: master, music, sound; Pad: glyphs, rumble; Bindings) and shows a one-line hint for the row under the cursor.
**Why**: The M4 plan's S54 row and its done-when, a settings screen a stranger can use: sections and hints are what a stranger needs, and the rest of the row (names, thumbnails, the question, rotation, the buses, the palettes) is each the smallest honest version. A daltonizing pass was chosen over recolouring content because every colour in the game is data an artist or a mod may change, and a pass follows them all.

## D-110 — Every string through one table; languages as content entries; a pseudo-locale that every screen must pass
**Date**: 2026-09-22
**Decision**: One static table, `Loc`: UI text by its English source (`Loc.t`), content text by `kind.id.path` (`Loc.content`, `Loc.text` for a top-level field) with a fallback by source text (`Loc.any`) for the places that hold a string without knowing its path (an effect's toast, a banter line, an epilogue), and missing always meaning the source. Languages are `locales` content entries ({name, ui, content}), so the base game's `en` is an empty table, a mod can ship a language by adding a file, and the settings screen lists what loaded. `Loc.TEXT_FIELDS` names the text fields of every content kind; `Loc.extract_content` walks them into keys, and `tools/extract_strings.gd` writes the whole template (every `Loc.t("…")` literal under `src/`, every content string) as one JSON to copy and fill. The `pseudo` locale is a transform, not a table: ⟦ ⟧ around every string that came through, every ASCII letter accented, format tokens and plain percent signs kept; `Loc.untranslated(text)` returns every plain letter run, inside a frame or out, so a name formatted into a translated frame is caught too. `tests/unit/test_localization.gd` switches to it and renders every screen the game has, failing on the first plain word with the screen and the word. Getting there routed some four hundred code strings through `Loc.t` (an automated pass over `lines.append`, labels, blurbs, hints, whys, toasts, returns and assignments with letters, then the rest by hand) and every content read the screens make (names at spawn, dialogue lines and choices in the runner, quest names, stages and objectives in the journal, blurbs and names at the Bastion, ability, class, race, item and affix names, endings, credits, saves' titles and map names, key and glyph names, ids shown as words). The player's typed name is the player's and stays as typed.
**Why**: The M4 plan's S55 row and its done-when: the game runs in pseudo-locale with no untranslated string. A pseudo-locale is the only test that proves the scaffolding is complete rather than present; making its checker strict (any plain letter, not just letters outside a frame) is what made it find the names formatted into frames that a looser check waved through. Locales as content entries keep the modding promise (a folder of JSON) and the validator's reach; a `Loc.any` fallback by source text was chosen over threading paths through every effect because the same source string translates the same wherever it appears.

## D-111 — Art integration without art: placeholders that say so, a lighting pass in the engine, screen effects as a setting, and the store's screenshots
**Date**: 2026-09-22
**Decision**: No sheet has arrived, so the session integrates what the pipeline promised and makes the absence visible. Every race and enemy now says what it draws with: `art.sheet` naming a sprite sidecar that exists and validates, or `"art": {"placeholder": "rig"}`; the validator refuses silence and a sheet that points at nothing, `tools/art_status.gd` (a CI step) writes `docs/art-status.md` from the registry (two sheets, trueborn and scav; the other nine races and twenty-four enemies on the rig; tiles, portraits, buildings and pickups generated at runtime; the four palettes), and the unit test holds that the doc names every race and enemy. Glow lives in the engine (GDD §5): a `LightingRig` under the map view with a CanvasModulate at the biome's `ambient` (a palette role, else `rules/lighting`), a PointLight2D on every tile whose art names a `glow` palette role (ten tiles: pools, conduits, pads, doors, the waypoint, in the neon of the biome) up to `max_lights` per map, and one light on the leader; a radial gradient texture built at runtime, additive blend, no shadows. A `ScreenFx` pass under the colour filter: glow (bright pixels bleed into their neighbours, a 5×5 bright-pass) by default, CRT (scanlines and a vignette on top) or off, and Lighting on or off, both settings with hints; `--fx=<mode>` and `--no-lighting` force them for a screenshot. The CI screenshot job renders the store page's missing shots (the road, a dialogue, the creator) and a CRT and a flat variant of the Shard.
**Why**: The M4 plan's S56 row and its done-when, every race and family with a sheet or a documented placeholder: documenting the placeholder in the entry itself, where the validator and a tool can read it, is what keeps it true as content grows. The lighting pass and the screen effects are the "palette and lighting pass" and "CRT and glow polish" the row names, built on data the sheets will also use (the tile `glow` role, the palette), so real art drops in without touching them. A budget of lights and a cheap bloom were chosen over a WorldEnvironment glow because the Deck runs the GL compatibility renderer, where 2D glow is not available, and because a per-map cap is what the performance budget can hold.

## D-112 — Audio integration without audio: files that drop in beside their sidecars, a music table by act and boss, and a status that names every placeholder
**Date**: 2026-09-22
**Decision**: No recording or composition has arrived, so the session makes the drop-in path real and the absence measurable. A recorded file beside its sidecar (`"file"` in the entry) now plays whether or not Godot imported it: an imported resource when it did, else read from disk by extension (.wav, .ogg, .mp3), which is how a mod's files arrive; music loops by default (`loop`), WAV forward over the file, OGG and MP3 by their loop flags; the director records for every entry whether a file or the synth played (`sources`, `is_placeholder`). The validator refuses a `file` that is not beside the sidecar or is of a kind the loader cannot read, and a `loop` that is not a bool. The music table can now say more than one track per state: a state maps to a track id or to a table read in order, a variant (`combat.boss` when a boss is engaged), `act<n>` from the story's flags, the biome, then `default`; the base game adds a Bastion track after the catastrophe, a boss fight track and a Loom Approach track, all placeholders. `tools/audio_status.gd` (a CI step) writes `docs/audio-status.md`: every entry, what plays for it, where the game fires it (the events table, the music table, abilities, tiles), the unused and the fired-but-missing; with `--strict` it exits 1 while any fired entry is still the synth, which is the plan's done-when as a command. `--play=<id>` auditions an entry in place. `docs/audio-pipeline.md` is the hand-off for a composer and a sound designer: formats, levels, the sidecar, where every sound is fired, the state machine, the gate.
**Why**: The M4 plan's S57 row and its done-when, no synthesised placeholder on a shipping path: that cannot be met without recordings, so the honest deliverable is the gate that will measure it and a path that lets each file land without a code change or an import step. The music table by act and boss is the "music state machine per act" the row names, built as data so the composer decides how many variants exist.

## D-113 — Steam as far as a checkout goes: a readiness report, a Workshop that round-trips without a client, thumbnails on the cloud, and a live checklist for the rest
**Date**: 2026-09-22
**Decision**: No app id, no Steamworks account, no client here, so the session ships everything the integration can prove alone and names the rest. `tools/steam_check.gd` (a CI step) writes `steam/READINESS.md`: the extension and the config, the twelve achievement API names as a copy-paste table for Steamworks, the twelve files the cloud syncs and a quota, the Steam Input template checked against every pad binding in `InputActions.BINDINGS` (a missing one is a blocker), the depot ids in `app_build.vdf`, the demo launch option, the installed Workshop items; `--strict` exits 1 while a blocker stands, and today three do (the extension, the app id, the depot ids). Workshop: `workshop_items()` and `workshop_publish()` on the platform contract; the Steam backend through the UGC API (`getSubscribedItems`, `getItemInstallInfo`, `createItem`, `startItemUpdate`, `submitItemUpdate`, awaiting the signals); the null backend to `user://workshop_null/<id>/` with a `workshop.json`, so subscribing, updating and loading run without a client; installed items are mod roots (`ContentRegistry.extra_mod_roots`, the registry reloading once at start when there are any); `tools/workshop_upload.gd` validates the folder with the mod validator and refuses a broken one before it publishes. Cloud: a save's thumbnail is pushed with it and the rotated autosaves are pushed too, so `pull_missing` brings back what the load page shows. `steam/CHECKLIST.md` is the ten-step live verification, one line in this log per step when it passes.
**Why**: The M4 plan's S58 row and its done-when, the demo and the full game on a Steam client: that needs the owner's account and cannot be done from here, so the honest deliverable is the report that says exactly what is missing, the code paths that run without the client, and the checklist that turns the rest into ten ticks. The Workshop through the null backend was chosen over a stub because the mod loader already takes any folder, so the whole path, publish, install, load, is exercised by a test today.

## D-114 — Apple as far as a checkout goes: notarization on Linux with rcodesign behind five secrets, the entitlements, a readiness report, a live checklist
**Date**: 2026-09-22
**Decision**: No Developer Program membership and no Mac here, so the session makes the signed and notarized build a matter of five repository secrets and leaves the rest to a nine-step checklist. The release workflow gains a step before the export that, when `APPLE_CERTIFICATE_P12`, `APPLE_CERTIFICATE_PASSWORD`, `APPLE_API_KEY_P8`, `APPLE_API_KEY_ID` and `APPLE_API_ISSUER` all exist, installs rcodesign (Apple code signing and notarization from Linux, no Mac in the loop), tells Godot where it is through the editor settings, flips the macOS preset to rcodesign signing and notarization for that run only, and passes the credentials through Godot's `GODOT_MACOS_*` environment overrides; a run without the secrets prints which are missing and exports the ad-hoc build exactly as before, so a fork keeps working. A verify step prints the signature after the export. The committed preset stays at signing disabled and notarization off by design, gains the reverse-DNS bundle id it already had, universal architecture, minimum macOS versions, versions in step with the project (0.3.0, which project.godot now also says), and `apple/entitlements.plist` as its custom entitlements: the hardened runtime with unsigned executable memory allowed (Godot's renderer needs it), JIT, dyld variables, library-validation bypass, audio and camera all off, no sandbox. `tools/apple_check.gd` (a CI step, strict) writes `apple/READINESS.md` from the preset, the plist and the workflow, and fails on a version out of step, a missing entitlement, or a workflow without the step; `apple/CHECKLIST.md` is the live verification: membership, certificate, API key, secrets, a tag, `spctl`, `codesign` and `stapler` on a Mac, a stranger opening it, `lipo` for both architectures, a quarantined download.
**Why**: The M4 plan's S59 row and its done-when, a macOS stranger opening the app without a warning: only a person with the membership can make that true, so the deliverable is the workflow that turns the credentials into a notarized build with no further code change, and the checklist that turns the rest into nine ticks. rcodesign was chosen over an Apple runner because the release workflow already runs on Linux and the game is GDScript, which needs nothing the hardened runtime withholds.

## D-115 — 1.0 close: the campaign test at every difficulty as the gate, the gaps triaged by who can close them, `v1.0.0` tagged as the open-source 1.0 with the Steam launch behind three hand-offs
**Date**: 2026-09-22
**Decision**: The campaign test now plays Act 1 six times, every difficulty in both death-stakes modes, and the four paths from each, twenty-four endings, plus the Iron Weave run: the gate the M4 plan named, green in CI on every tag. The versions move together to 1.0.0 (project and macOS preset, which the Apple check holds equal) and the title line drops "demo". `docs/gaps.md` is triaged at the end of M4 with a verdict at its head: the code and the content are complete and gated, every lever is data, three status tools say what is still generated or unverified, and everything that remains open needs a person other than a session, each with a hand-off doc, a checklist and a gate. `docs/m4-plan.md` carries the exit criteria with the three that are the code's ticked and the three that are people's not. `docs/release-notes/v1.0.0.md` says what is new since v0.3.0 and, plainly, what 1.0 is and is not; `v1.0.0` is tagged and `release.yml` builds and publishes it. The store page draft is complete and goes live with the app id.
**Why**: The M4 plan's S60 row says `v1.0.0` and "Launch"; the launch it means, on Steam with art and audio, cannot be made by a session, and the owner chose to run M4 without the playtest and the hand-offs. Tagging 1.0 of the open-source game is honest about what it is (complete, tested, buildable, moddable) as long as the notes and the gaps say what it is not, which they do; calling it a release candidate instead would have hidden the same facts behind a suffix and left the roadmap's last row unfilled. The gate is the campaign test at every difficulty because it is the one claim about 1.0 a session can prove.

## D-116 — The engineering gaps closed: every binary executed, a crash report from the log, caps on mod media, a required screenshot check, tests off the player's files, one engine version
**Date**: 2026-09-24
**Decision**: The six engineering rows of the v1.0.0 gap analysis, in one session. The release workflow gains `smoke-windows` and `smoke-macos` jobs that download the export job's archives, unzip them and boot the game headless with `--quit-after`, so a build that cannot start on its platform fails the tag before it publishes (Windows also tries a rendered title frame through ANGLE, best-effort); the GitHub Release moves to a `publish` job that needs all three smokes. Crash reporting without a service: the engine now writes its log to `user://logs/` (project.godot), `CrashReport.begin()` at start reads a marker the previous run left and, if that run never called `end()` (the title's Quit and the window's close do), copies the last 200 log lines to `user://crash_<stamp>.txt` and the title toasts where it is and to send it with the playtest log; nothing leaves the machine by itself. Mod media: `MediaLimits` caps images at 8 MB, 4096 pixels a side and a PNG signature, audio at 32 MB and the three extensions, checked before decoding in the sprite-sheet loader, the audio loader and the validator. The screenshot job is required now: `tools/check_shots.gd` fails it on a frame that is missing, under 1280×720, blank (under 2% off the clear colour) or flat (under 64 colours), and the duplicated shot rows from S56 are gone. A world created under tests with the default account, ledger or saves path is redirected to a `test_*` path, so no test can write the player's files again. The engine version is checked once: a test holds project.godot's feature tag, both workflows' `GODOT_VERSION`, the notarize step's editor-settings file and the running engine to the same major.minor, and the macOS preset's version to the project's.
**Why**: These were the gaps a session could close without a person, and each one turns a silent failure into a red job or a file with a name: a build that does not boot, a crash nobody could describe, a mod that could hand the loader a gigabyte, a menu that broke while CI stayed green, a test that quietly changed the player's settings, a version that drifted in one of five places.

## D-117 — Gore as a setting over a decal layer the world keeps as data; grime and failing lights as dressing every Shard scatters last
**Date**: 2026-09-24
**Decision**: The GDD's gore slider (§9) and its mature environment layer (§5) in one session (S61). Gore is a Display setting with three levels, full by default: the game is the gritty one the GDD describes, and the slider is for turning it down. What the setting governs is a `DecalLayer` under the walls that draws marks the world owns: a splat where a hit landed, a pool where someone went down or died, a body where someone died, each a small dictionary (cell, kind, seed, fluid, colour) so it is data a save carries; handcrafted maps keep theirs in the story (`narrative.marks`), Shards in the run (`run.marks`, gone with extraction or a wipe), both capped at 200 a map with the oldest dropped. Low keeps the bodies and pools, smaller and fainter, and no splats; off draws nothing and the fallen fade as before. Marks are recorded whatever the level, so turning gore up shows what the fights left. The fluid is a trait on the enemy (`traits.fluid`: blood by default, oil for drones, turrets and constructs, none for wisps), validated. Grime and failing lights are not gore and no setting governs them: a `grime` floor pattern and tile and a `flicker` flag on a glowing tile (its light gutters on two beats that never line up, with a stutter) are scattered over every Shard's plain floor at the very end of generation with their own RNG seeded from the Shard's, so every layout and placement before it is exactly what it was, and placed by hand in the yard.
**Why**: The pillar "gritty and mature" was carried by text alone. The cheapest thing that makes a fight leave a trace is a mark on the floor, and making the marks data rather than nodes means they survive a save, a door opening (which rebuilds the map view) and a settings change without special cases. The dressing goes last with its own RNG so that no Shard seed anyone has tested changes; the yard gets a few by hand because the screenshot job looks at it.

## D-118 — Account unlocks beyond origins: loadouts as content, cosmetics as flagged appearance options, one grant path for every kind
**Date**: 2026-09-24
**Decision**: GDD §12 names three account-level unlocks: origins, loadouts, cosmetics. Origins existed (D-090); this session (S62) adds the other two on the same mechanism rather than a new one. A loadout is a `loadouts` content entry: items, optional resources, an optional `unlock_flag` and `unlock_blurb`, and `default` on exactly one. On a new game the chosen loadout's items are worn where they fit the protagonist's slots (weapon, armour, two trinkets, cyberware for a Chromed), the rest go to the pack, the resources are banked, and the account counts the playthrough. The default kit is empty-handed on purpose: every test, harness band and balance number from before S62 stands, and the kits are a choice rather than a buff every run starts with. A cosmetic is a tone or accent in `rules/appearance` carrying the same `unlock_flag` / `unlock_blurb` pair; the creator offers it once the account holds its key and lists it as locked with the blurb until then. `Account.earnable` walks origins, loadouts, tones and accents in that order so every kind is granted at the same autosave and toasted by name through `Account.key_name`; keys are `origin:`, `loadout:`, `tone:`, `accent:`. The creator gets a Loadout row after Class with the kit spelled out, and a header line with the account's playthroughs and unlocks earned of those offered.
**Why**: One mechanism (a flag on an entry, a key on the account) is what a modder can extend without code and what the validator can check: it already refused a missing origin flag and now refuses a kit with an unknown item, a resource that does not exist, an empty flag or two defaults. Keeping the default kit empty avoids the alternative, retuning the harness for a stat every new game would carry.

## D-119 — Hacking as one ability effect over a trait: turrets turned for the fight, gates opened by Tech
**Date**: 2026-09-24
**Decision**: GDD §9 lists "hackable turrets/doors" among the combat surfaces. Both land on things that already exist (S63). In combat, Hack is an ability like any other (content, 2 AP, adjacent, accuracy 60, effect `hack`) that the controller hands out per fight rather than the class: to the protagonist when the sheet's Tech meets `rules/combat.hack_tech_min`, with `hack_tech_bonus` on the roll for every point past it, and to every member whose class carries the Tech branch (Drone Shepherd, Circuit Witch, Wireghost) at the base roll. It only targets a combatant whose entry says `traits.hackable` (the shepherd turret, the feral drone, the warden construct) on the other side; a hit changes the target's team, so the player controls it, enemies turn on it and the outcome check counts it as ours; when the fight ends it powers down and drops nothing, so a hack is never a loot shortcut. In exploration, a map's locked `doors` entry may name `hack_tech`: the protagonist's Tech at or past it opens the gate without the key flag and sets the same `opens_flag` the key would, so the story past the gate reads the same; the HUD line says what the gate takes and what the party has.
**Why**: An effect over a trait is the pattern the surfaces already use (tile data, a rules number, a hook in the state), and it keeps the AI honest: an enemy with the ability could hack a party drone the same way. Handing the ability out per fight instead of writing it into six class files keeps the class data as it was and lets the rules number, not code, say who is Tech enough. Doors take a threshold rather than a roll because a failed roll on a gate is a reload, not a decision.

## D-120 — Awareness as a cone with hearing behind it, a meter with a roll at the top, and an opener for striking the unaware
**Date**: 2026-09-24
**Decision**: D-025 kept awareness as a radius and promised a cone and noise model once stealth landed; S64 lands it without moving a single existing encounter. A map placement may give an enemy a `facing` (eight names, with an optional sweep that swings it back and forth); only then does its awareness become a cone (`rules/combat.vision_cone_degrees`, 180° by default) with a short hearing radius behind it. A placement without a facing looks all round exactly as before, which is every Shard enemy and every handcrafted placement except the yard's chrome-addict, so no harness band, campaign path or Shard seed changed. Noticing has two speeds: within `notice_instant_radius` (2 cells) a seen party member starts the fight at once; farther out, each frame in view fills the enemy's meter over `notice_seconds` (faster the closer), each frame out of view empties it over `notice_decay_seconds`, and when it fills the enemy rolls `detect_base` (0.7) plus `detect_per_awareness` for every point of awareness over the default minus `detect_per_stealth` for every point of the party's best `stealth` trait, never under 5%; a failed roll is a second look (the meter drops to half). A settled look, which is what `check_encounters()` without a delta means and what tests, teleports and the harness call, is the old rule: seen is noticed. The HUD shows the fullest meter. Clicking an enemy whose meter is under full is an opener: `start_combat(true, true)` hides every standing party member for one turn, so the first blow each lands is an ambush under the rules that already existed for hiding. The `stealth` trait joins the D-085 vocabulary; the Scav Runner origin has 1.
**Why**: The cone only applies where content asks for it, because a cone on every procgen enemy would silently change every balance number the harness holds, and a modder or a map author is the one who knows which way a sentry faces. The roll happens once at the top of a meter rather than every frame so the outcome is legible: the player watches the meter and knows the moment of truth. The opener reuses hiding rather than a new bonus so Story difficulty's softer ambush multiplier already applies to it.

## D-121 — Positioning: a climb costs Move, leaving a fighter's reach draws its strike, archetypes weigh the ground their own way, hidden means unseen, placed enemies patrol
**Date**: 2026-09-24
**Decision**: The systems row the gap analysis rated S2 ("positioning matters less than the pillar wants") closes in one session (S65). Movement is a shortest-path flood in which a step up onto a taller tile costs `rules/combat.climb_move_cost` more, so a catwalk is a choice with a price and the path goes round when going round is cheaper. Leaving a cell beside an active, unhidden hostile that has a reach-1 damaging ability, for a cell that is not beside it, hands that fighter one free strike per move: a plain roll with its melee ability, no AP or resource, no chain, no counter, emitted as an `opportunity` event so sound, rumble, decals and the log treat it as a hit. A mover put down on the way stops where it fell. The move can still be undone; the strike stands, because the damage is already real and undoing the position is not an exploit. The hover hint says the cost, whether it is a climb, and how many free strikes the path draws, and the AI's approach key counts each as `ai_opportunity_penalty` lost texture so enemies do not walk through the party's reach for a slightly better cell. `ai_archetype_weights` scales the four texture weights per archetype (ranged and stealthers value cover, controllers value height and pools, rushers barely care) as a nested table in the rules so a difficulty overlay can retune it. A hidden enemy is drawn at alpha zero unless a standing party member with `detect_hidden` could sense it; a hidden party member stays faint to the player. A map placement's `patrol` is a loop of cells walked at `patrol_speed` with `patrol_wait` at each point; the facing follows the walk so the cone looks ahead, a patrol waits rather than walks into anyone, and one whose meter is up stops and turns to what it saw. Patrol positions are not saved: a load puts the enemy back at its placed cell.
**Why**: Each piece uses a channel that already existed (the flood, `_strike`, the event stream, the texture score, the facing from S64) rather than a new subsystem, so every rule is one number in `rules/combat` and every effect is one more event kind the controller already routes. The free strike is once per fighter per move and never from hiding so it cannot chain into a stun-lock, and it is a rule flag so Story can switch it off.
**Balance note**: the harness moved with the rules. Enemies that walked out of the party's reach were dying to free strikes, so the AI learned two things in the same session: a pinned enemy (a melee fighter beside it) attacks that fighter from where it stands when it can, and a firing cell reached without drawing a strike outranks one that draws it. With that, every story row sat in its band but the gallery, which had crept onto its ceiling before this session (19 of 20 with every S65 rule off) and tipped over: its cantor and its hollow are elites now, 95% naive wins in the harness; the Act 1 walkthrough takes the Vane nest by fiat, the fight its naive loop no longer wins.

## D-122 — Progression v2: the second class gets a subclass and the loop follows the levels, the next level is previewed, talents change abilities, capstones have a line
**Date**: 2026-09-24
**Decision**: D-086 left four holes in progression and named them; S66 closes them without a new screen. The second class of a multiclass may take one of its own subclasses once it holds `subclass_level` levels, stored as `build.multiclass.subclass` and applied exactly as the main subclass is (abilities, stat mods, damage bonus); the Arcanum resets it with the rest. A combatant still holds one resource, so the loop follows the levels: the member runs the resource of whichever class holds more, the second on a tie. That makes the split a build decision with a visible consequence rather than a second meter bolted onto every code path that reads a resource, and the Weave header says which loop is running. `Progression.preview` diffs the build's effects at the next level, assuming the level goes to the main class, and the header states the stats, the unlocks, the talent tier, the subclass and the capstone that level would buy, or "at the cap". Talents may carry `effects.traits`: a dictionary merged with race and origin traits through the same merge, so `ability_range_bonus`, `ability_cooldown_bonus`, `ability_damage_bonus`, `ap_bonus`, `resist`, `stealth` and the rest reach the fight through the channel that already carried `ability_damage_bonus`; the validator refuses any other key and any ability that does not exist. Every class carries a `capstone_line`, toasted and written to the history the moment the eighth level lands in one class.
**Why**: One resource per combatant is what every cost, overload, vent, absorb and stack rule assumes; two loops at once would have meant touching every one of them for a feature the GDD describes as a capstone reward, not a base mechanic. Making the loop follow the heavier class gives multiclassing a real choice at level 12 (six and six flips it) for the price of one function. Traits were already the vocabulary races and origins use to change fights, so talents joining it needed no new reader, only a validator that says which keys are allowed.

## D-123 — The races' mechanics: a Synth repairs with parts, a Skyborn reads the ruin, the Swarmborn face is a roll, every origin has a trait
**Date**: 2026-09-24
**Decision**: Three races and five origins had identity text and no verb (S67). Each gets one through the trait vocabulary (D-085) rather than a new system. A Synth carries `repair_with_parts` ({salvage, heal}): a pause-menu action anywhere out of a fight that spends salvage for a fraction of max HP and stands a downed Synth back up; and because "nothing arcane healing can find" it, the Garden skips anyone whose `heal_immune_types` names arcane while the tech med-bay and medkits still work. A Skyborn carries `secret_sight` (secret doors open from that many cells, with a clear line, for the whole party) and `read_lore` (a fragment is read where it lies: its text goes to the history and the screen, and the Archive still assembles it). The Swarmborn disguise is rolled once per talk: `disguise_base_chance` at the arcane minimum plus `disguise_per_arcane` for each point above it, with the seeded world RNG, so a save reproduces it; the first node of the talk reads the roll, a toast says whether the face held, and outside a talk the old threshold stands so triggers behave as before. Every origin carries a trait: the Choir-touched hear the hidden, corporate training makes talents cheaper, a vault child mends a little after every fight, a Beacon keeper knows one cell of the way down, a scav runner moves quietly.
**Why**: A trait is data a race or origin already carries, a modder can already write, and the fight and the world already read; the session adds three readers (repair, secret sight, lore) and one roll, and the validator learns the two shapes that could be wrong. The roll is per talk and seeded so a disguise is a gamble the player can feel without being a reload loop, and the old threshold outside talks keeps every trigger condition deterministic.

## D-124 — Saves: F5 asks, F9 offers, the autosave depth is a setting, and a fight rides the autosave
**Date**: 2026-09-24
**Decision**: The saves row of the analysis had three parts (S68). F5 now saves straight to slot 1 only when the slot is empty; over an existing save it opens the saves screen with the overwrite question already on the slot, so the same Enter that confirms on the screen confirms here, and Esc keeps the old save. F9 opens the load page with the cursor on slot 1, one confirm away, and says so when the slot is empty. How many older autosaves stay is a Saves setting from one to five, passed to the rotation and the listing, with the copy past a lowered setting dropped on the next autosave; Iron Weave keeps one whatever it says. The fight rides the autosave: a combatant, the state (order, group, turn, round, the RNG's state, the log's tail) and the controller's actor map (a party member by id, an enemy by its index in the world's roster) each turn into data, the world writes its enemy roster beside them so trigger-spawned enemies and the dead gaps between indices come back exactly, and a load with a `combat` block rebuilds the fight on the restored map and hands control to whoever was to act. Saves are v3 with a no-op migration; slots still refuse to save mid-fight; the window's close button writes the autosave mid-fight, so a quit resumes where it stood and an Iron Weave run walked out of no longer restarts from its checkpoint. A resumed fight rolls what it would have rolled, because the RNG's state is saved, not only its seed.
**Why**: Asking on the screen and asking on the key are the same question, so the key opens the screen rather than growing a second dialog. The autosave depth was already a loop over a constant; a setting is the constant with a row. The mid-fight save was the expensive one, and it is data all the way down: nothing in the fight was a node except the actor map, and that map is two kinds of reference. Writing the roster beside the fight rather than reusing the placement list is what makes trigger-spawned enemies survive a load at all, which the pre-fight checkpoint never managed; the checkpoint stays as the fallback for a crash.

## D-125 — Movement: nobody stands in anybody, the party steps into its cells, the pad cursor glides
**Date**: 2026-09-24
**Decision**: The last systems row (S69) is four small rules on the movement that already existed. The leader's steering asks the world whether a spot can be stood on, and the answer is walkable and nobody but the party there, so an enemy or an NPC is a wall to WASD; a click on an occupied cell is refused and the path search treats the occupied cells as walls for that search only (`NavGrid.find_path` takes a blocked list and unblocks after), so the party walks round Sera rather than through her. Followers hold where they are when their next step would come within `Party.MIN_GAP` of anyone ahead of them in the line or onto anyone at all, which is what keeps three people from occupying one cell without a physics body. When a fight begins and the fight animates, the party steps onto its settled cells over a beat instead of snapping; under tests and screenshots it still snaps. The pad cursor's drawn diamond glides toward its cell at a fixed speed, the first step landing it and any mouse motion clearing it; the cell layer the tests read is unchanged.
**Why**: Collision here is a question of who may stand where, and the world already knew (enemy_at, npc_at, is_walkable); giving the party a callable to ask is smaller and more honest than physics bodies on placeholder capsules, and it keeps the deterministic tick the tests and the harness drive. A gap between followers is the same question asked of the line ahead. The settle tween and the cursor glide are the two places a snap was visible to a player and to nobody else, so both are visual only and both are off in headless runs.
