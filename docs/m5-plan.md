# M5 plan — The promises

Written after `v1.0.0` (2026-09-24), from `docs/gap-analysis-v1.0.0.md`. M4 shipped the open-source 1.0; the analysis then read the GDD against the code and found the rows below that were never started. M5 closes every row a session can close and leaves the rest with a named owner in `docs/gaps.md`.

## What M5 is for

1. **Every mechanic the GDD names exists in the game**: the gore slider and the mature dressing (§5, §9), account unlocks past origins (§12), hackable turrets and doors (§9), awareness as a cone with a detection roll (§9).
2. **The pitch says what exists.** The campaign is four to six hours on a first run; the store page and the README say so until a writing pass changes it.
3. **Engineering keeps the bar**: the D-116 gates (smokes, screenshots, media caps, one engine version) stay green through every session.

4. **The systems section of the analysis** (§6): the rows a session can close, one each, added on 2026-09-24 after the GDD promises landed: positioning in combat, the AI's texture, progression v2, the races' mechanics, saves, movement. The rows that need a person (a Deck, colour-blind testers, a translator) keep their owner in `docs/gaps.md`.

## Sessions

| Session | Work | Done when |
|---|---|---|
| — | **Engineering and process** (done first, D-116): every binary executed, crash reports, mod media caps, a required screenshot check, tests off the player's files, one engine version | Done 2026-09-24 |
| S61 | **Gore slider and the mature layer**: a `gore` setting (off/low/full); blood splats where actors are hit and downed in combat, on the palette's `blood`; corpses left on the map after a fight; grime and failing lights as tile dressing (always on; they are not gore) | A fight leaves marks the setting can turn off; the screenshot job shows them |
| S62 | **Account unlocks, loadouts and cosmetics**: `loadouts` and `cosmetics` content kinds with an `unlock` key each, granted by the same achievement path as origins, chosen in the creator | A second run can start with a kit and a look the first one earned |
| S63 | **Hackable turrets and doors**: a `hack` interaction on locked doors (Tech check) in exploration and on turret-kind enemies in combat (adjacent, an action, a Tech roll; success disables or turns the turret) | The yard's locked door and the shepherd turret both answer to Tech |
| S64 | **Awareness as a cone and a detection roll**: enemies face a way, see a cone that walls block, roll perception against the party's stealth each tick into a noticed meter; stealth openers keep working; the HUD shows the meter | A party can walk behind a patrol; the opener test still ambushes |
| S65 | **Positioning**: attacks of opportunity, a movement cost for climbing, per-archetype texture weights, hidden units faint only to the side that can see them, patrol routes for placed enemies | Leaving a melee threat costs a hit; a climb costs move; each archetype weighs the ground its own way |
| S66 | **Progression v2**: a subclass and a resource loop for the second class of a multiclass, a preview of the next level on the Weave, talents that change abilities, a line for every capstone | Every level shows what it buys before it is bought |
| S67 | **The races' mechanics**: a Synth repairs with parts, a Skyborn's knowledge reads the ruin, the Swarmborn disguise is a roll, origins do something mechanical | Each of the three has a verb, not a tag |
| S68 | **Saves**: F5 asks before it overwrites, autosave depth as a setting, a mid-combat save so an Iron Weave fight walked out of resumes where it stood | A fight survives a quit and a crash |
| S69 | **Movement**: actors block each other, followers do not overlap, the party settles into combat cells with a step, the pad cursor glides between cells | Nobody stands in anybody |
| S70 | **Balance measured further**: the Key keepers rowed in the harness, every story row measured on Story and Tactician with two general difficulty knobs, the depth numbers re-read after positioning | Every fight the campaign has, and every difficulty, has a naive number and a band |
| S71 | **Maps remember and gates ask**: dead placed enemies stay dead across map changes and saves; a gate walked onto asks, a click or a confirm goes | Leave the yard and come back: the scav you killed is still down |
| S72 | **Consequences past the door**: resisted and benched as ending fates in every ending, `walking` and `benched` conditions, late-game words and room states per faction | A companion who stood on the plaza is read back as having stood |
| S73 | **Items v2**: consumables in the pack and on the action bar, biome drop tables through families, sell-back, cyberware that costs parts to fit | A medkit is a thing you carry and choose when to open |

## Constraints

- One system per session, data-driven, tested, never breaking exports; each session ends with a merged PR with green CI and its D-number.
- **The campaign** (30+ hours, GDD §1, §11) is a writer's, not a session's: the data format takes lines without code. It stays in `docs/gaps.md` under "Needs a person".

## Progress
- S61 Gore slider and the mature layer — done: `gore` as a Display setting (full by default; low keeps the fallen and little blood; off cleans every fight away) pushed to a `DecalLayer` under the walls that draws marks the world keeps as data (blood splats on hits, a pool on a down or kill, a body on a death, capped at 200 a map; handcrafted maps hold theirs in the story, Shards in the run, both saved); `traits.fluid` on enemies picks blood, oil (drones, turrets, constructs) or none (wisps); a `grime` floor pattern and tile plus failing lights (`art.flicker` on a glowing tile: its light gutters on two beats) scattered over every Shard's plain floor last with their own RNG so layouts stand, and placed in the yard. D-117.
- S62 Account unlocks, loadouts and cosmetics — done: `loadouts` content (Travelling light by default and empty-handed so nothing balanced moves, Scav kit, Corp issue free; Keeper's cache by first contact, Loom-walker's kit by any ending) with items worn where they fit the protagonist's slots and the rest in the pack, resources banked; tones and accents carry the same `unlock_flag` / `unlock_blurb` pair (Choir silver, Loom-lit); `Account.earnable` grants origins, loadouts, tones and accents by one path, `key_name` names the toast; the creator has a Loadout row with the kit spelled out, lists locked kits and looks with their blurbs, and shows the account's playthroughs (counted on every new game now) and unlocks earned. D-118.
- S63 Hackable turrets and doors — done: `hack` as an ability with effect `hack` (2 AP, range 1, accuracy 60) handed out per fight to the protagonist at `rules/combat.hack_tech_min` (2) and to every member whose class has the Tech branch, the protagonist adding `hack_tech_bonus` (15) per point past the minimum; a hit turns a target with `traits.hackable` (shepherd turret, feral drone, warden construct) to the caster's side, the turned machine is player-controlled, counts for victory, powers down when the fight ends and drops nothing; a map door with `hack_tech` opens to that much protagonist Tech without its key, setting its opens_flag, with the HUD saying what it would take (gate road and throat doors at 3). D-119.
- S64 Awareness as a cone and a detection roll — done: a map placement's `facing` (eight names, optional `sweep`/`sweep_period`) makes an enemy see a cone (`rules/combat.vision_cone_degrees` 180) and only hear within `hearing_radius` (2) behind it, no facing looking all round as before; `check_encounters(delta)` notices at once within `notice_instant_radius`, farther fills the enemy's `noticed` meter over `notice_seconds` scaled by closeness, empties it over `notice_decay_seconds`, and at the top rolls `detect_base` + per awareness point − per point of the party's best `stealth` trait (a Scav Runner has 1), a failed roll dropping the meter to half; a settled look (no delta: tests and teleports) is the old instant rule; the HUD shows the fullest meter; clicking an enemy whose meter is under full is an opener (`start_combat(true, true)`: every party member hidden for a turn, the first blows ambushes). The yard's chrome-addict looks north and sweeps. D-120.
- S65 Positioning — done: the movement flood is a shortest-path search where a step up in `height` costs `climb_move_cost` (1) more, the path goes round a hill when that is cheaper, and the hover hint says the cost and when it is a climb; leaving a cell beside an active, unhidden hostile with a reach-1 damaging ability for a cell not beside it draws that fighter's free strike once per fighter per move (`opportunity` events, marks and rumble like any hit; a mover put down stops where it fell; the move can still be undone but the strike stands; `opportunity_attacks` is a rule a difficulty may turn off); `ai_archetype_weights` scale cover, elevation, corrosive and mana-pool weights per archetype and the approach key counts each free strike a path would draw as `ai_opportunity_penalty` lost texture; a hidden enemy draws at alpha 0 unless a standing party member with `detect_hidden` could sense it, a hidden party member at 0.45; a placement's `patrol` walks a loop of cells at `patrol_speed` with `patrol_wait` at each point, the facing following the walk, waiting rather than walking into anyone, and stopping to face what it notices. D-121.
- S66 Progression v2 — done: `build.multiclass.subclass` (a subclass of the second class, chosen on the Weave once the second class holds `subclass_level` levels, its abilities and mods applied like the main's); `Progression.resource_class` (the loop of whichever class holds more levels, the second on a tie) sets the member's resource and the header says which; `Progression.preview` and `preview_line` show the next level's stats, unlocks, talent tier, subclass and capstone before it is bought; talents may carry `effects.traits` (range, cooldown and damage per ability, extra AP, resist, stealth) merged like race traits and read by the fight (four shipped: Long Arc, Cold Barrel, Sure Hand, Second Wind), validated key by key; every class has a `capstone_line` toasted and logged when the eighth level lands. D-122.
- S67 The races' mechanics — done: `repair_with_parts` on the Synth (a pause-menu action out of combat: salvage for a fraction of max HP, a downed Synth stands up with rivets; the arcane Garden skips anyone with `heal_immune_types` arcane, the tech med-bay does not); `secret_sight` and `read_lore` on the Skyborn (secret doors from two cells with a clear line; a fragment read aloud into the history and toasted where it lies); the Swarmborn disguise rolled once per talk (`disguise_base_chance` at the arcane minimum plus `disguise_per_arcane` above it, the seeded world RNG, a toast for the face holding or slipping, the old threshold outside talks); every origin with a trait (Choir-touched detect_hidden 2, Corp Asset talent_cost_mod −1, Vault Child mend_after_combat 0.1, Beacon Keeper secret_sight 1, Scav Runner stealth 1). D-123.
- S68 Saves — done: `quick_save` goes straight to slot 1 the first time and otherwise opens the saves screen with the overwrite question on the slot; `quick_load` opens the load page with the cursor on slot 1; `autosaves_kept` is a Saves setting (1 to 5) that `rotate_autosaves` and `list_saves` take, dropping the copy past a lowered setting; the fight rides the autosave: `Combatant.to_dict/from_dict`, `CombatState.to_dict/restore_from` (order, group, turn, round, RNG state, log tail), `CombatController.to_dict/resume` (actors as member ids or roster indices), `world.enemy_roster/respawn_roster` (trigger-spawned enemies and dead gaps included), `save_fight` on the window's close mid-fight, saves at v3 with a no-op migration; a resumed fight rolls what it would have rolled. D-124.
- S69 Movement — done: `can_stand_world` (walkable and nobody but the party there) steers the leader and answers for the followers through `Party.can_stand`; `NavGrid.find_path` takes blocked cells and `command_move` refuses an occupied cell and paths round `occupied_cells()`; followers hold when their next step would come within `Party.MIN_GAP` of anyone ahead or onto anyone; `settle_members` tweens the party onto its combat cells when the fight animates and snaps otherwise; the highlighter draws the e_cursor layer at a `glide` position that slides toward the cell at 900 px/s, the first step landing it and the mouse clearing it. D-125.
- S70 Balance measured further — done: `rules/combat.enemy_hp_mult` and `enemy_damage_bonus` (1.0 and 0 by default; Story 0.85; Tactician 1.2 and +1) applied to every enemy before tier and depth in `StatBlock.for_enemy`; `test_balance` rows the three Key keepers (the site dialogue inside the Key's Shard, "Take it whole", level 9, depth 2, geared: Quiet 60%, Ledgers 75%, Heart 90% with its construct an elite) and plays every story row on Story and Tactician (ten seeds), holding Story ≥ Balanced − 0.1, Tactician ≤ Balanced + 0.1 and a per-row Tactician floor; the gaps table carries the numbers as they are after S65. D-126.
- S71 Maps remember and gates ask — done: `narrative.map_kills` (map id → placement indices, written by `remember_dead` at every victory on a handcrafted map, applied in `spawn_enemies`, saved with the story); `check_transitions(confirmed)`: the frame loop passes false and a gate under the leader shows "label — Enter / A to go" on the HUD and waits, `interact()` confirms, a `command_move` onto a gate sets `gate_intent` so the arriving frame goes through and walking off forgets it; tests and scripts call it confirmed and see no change. The credits already said what is true. D-127.
- S72 Consequences past the door — done: `Endings.fate_key` gains `resisted` (above romanced) and `benched` (above alive), all five endings write both for every companion who can have them (twenty resisted lines, thirty benched); `walking` and `benched` join the condition vocabulary (validator, lists in the tests); each `*_path_report` gets `keys` and `loom` start nodes, each `*_intro` a `late` node, each faction area two once-only arrival triggers (`after_keys`, `after_catastrophe`). D-128.
- S73 Items v2 — done: slot `consumable` with a `use` block ({heal} or {ap}); `use_item` out of a fight (shared round at half strength) from the pack screen (`use` rows) and the pause menu (`use_<item>` rows with a count); in a fight `ItemSystem.consume_ability` gives every party member a `use_<item>` action per consumable type in the pack, the state applies the block (`consume` event), the controller spends one instance and drops the action at zero; consumables never roll with affixes; `families` set on seven bases and crates pass the Shard's biome; `supply_cache` pickup (a named `grants.item`) in every Shard pool; the fence sells both; merchant rows sell back at `loot.sell_fraction` plus `sell_rarity_bonus` per rarity rank; cyberware `install` costs spent on equip; validator shapes. D-129.
