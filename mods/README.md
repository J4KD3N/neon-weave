# mods/ (development mod root)

Drop a mod folder here to test it from the editor. This folder is one of the
three roots the registry scans (`res://mods`, `user://mods`, and `mods/`
beside the exported executable). Mod folders here are git-ignored except this
README and `example_mod/`.

See `docs/modding.md` for the format.
